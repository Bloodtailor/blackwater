# BLACKWATER — Build Plan

> **STATUS — M14.5 shipped: THE ROSTER OF 41. Every Drowned is now a man from the crew book (`src/zombies/roster.ts`, internal, seeded from T6's read): his own name, role, build, faded palette, ONE gear prop, moan pitch, pace, and habits — one of each, EVER (a corpse holds his slot until it settles; proven live when Bell's drifting body refused his own respawn). Carriers always visibly carry and always drop — Barrow's dry-cell, Bell's ammo, Marsh's output slug — and balance lives in the watch bill (they walk 0.35/0.3/0.12 weights vs 1.0). Lowe's "Barrow, was it" only plays once Barrow has truly walked 3 watches. 160/160 tests. ⚠ Perf honesty: with the preview pane hidden the canvas is 0×0 and ALL render benches are noise (M14's 0.64 ms included) — forced 720p on the real Intel UHD reads ~14 ms at 22 alive ≈ 60–70 fps; check the FPS counter on a real screen. NEXT: M15 (Lamp Man + Angler rework). 🎮 USER: play a few shifts and DON'T read the roster — see if you start recognizing men on your own (the big one with the chest lamp is Barrow; killing him pays a battery). 🎧 Still queued: in-head Lowe / robotic REMORA / shift bell / per-man moan pitch listening.** Update this line every session.
> **DESIGN REOPENED 2026-07-21 (user redesign round), rewritten sections are authoritative:** economy → found items (DESIGN §10), rounds → time-based shifts + wandering population (§9), Angler rework + Lamp Man (§8), Museum/concept gallery (§12.1), in-head VO + music/voice arbitration (§14), LORE v3 (spread the wrongness; no new count material). Outside those sections the old lock discipline holds: implement, tune, fix — log forced deviations loudly. **CANON.md now exists: every player-visible addition gets its row there in the ship session.**

## Session protocol (follow this every prompt)

1. Read DESIGN.md and this file — plus LORE.md before building anything player-visible (it dictates every asset's identity and appearance), plus CANON.md when touching lore-adjacent content (and add rows there for anything you ship). Trust the docs over memory of past sessions; trust `src/cave/data.ts` over both for layout specifics.
2. Smoke check: `npm run dev` via the preview pane, load the game, glance at console. If the repo is broken, fixing it *is* the session until green. (Skip pre-M0.)
3. Do the **next unchecked milestone**. One milestone per prompt. If a milestone turns out too big, split it *in this file* mid-session and finish the first half properly — never rush the back half.
4. Every feature lands with its `?debug=1` trigger in the same milestone.
5. **Verify the Definition of Done in the browser** — teleport to it, trigger it, screenshot it. Check the FPS counter. Only then check the box.
6. Update the STATUS line, append a Worklog entry (3–6 lines: what shipped, what I learned, what I changed in the docs and why). Update DESIGN.md if a design decision changed — intent changes only, not number nudges.
7. Commit (small WIP commits mid-milestone are encouraged; final commit at milestone end). Never commit `.env`. Repo must run cleanly before the session ends.
8. Rules of the codebase: DESIGN.md §16. Non-negotiables: map-as-data, all numbers in `tuning.ts`, no new deps without a note here.

## Milestones

### M0 — Scaffold & harness ✅ 2026-07-18
- [x] Vite + TypeScript (strict) + Three.js; `npm run dev`; `.gitignore` covers `node_modules`, `dist`, `.env`
- [x] `.claude/launch.json` so the preview pane can start the dev server
- [x] Render a placeholder chamber (displaced-noise sphere), water fog, FPS counter
- [x] Freefly noclip camera; `?debug=1` panel skeleton + hotkey framework; screenshot-friendly (HUD-less) mode
- [x] `src/tuning.ts` created; Vitest wired with one trivial test (3 invariant tests)
- **DoD met:** chamber renders at 0.03 ms/frame (≫60 fps); WASD/vertical movement verified to exact spec (8.00 m/s) via harness-stepped frames; debug panel + F1/H hotkeys verified; screenshots in `docs/screens/m0-*.png`; tsc clean, tests pass.

### M1 — Cave data & map viewer ✅ 2026-07-18
- [x] Schema from DESIGN §16; full layout authored in `src/cave/data.ts` — 62 nodes / 74 edges, all 5 zones, every tag placed (burrows ×11, pockets ×8+surface, mounds ×6, tilt edges, squeezes ×9, doors ×6 at DESIGN costs, buys, 9 perks, 3 box spots, power, PaP, Heart, tapes ×6, caches, tie-offs, posters ×10, toys ×3 in dead ends, jukebox)
- [x] `?view=map`: labeled 3D wireframe (freefly) + top/side blueprint canvases with legend, door costs, tilt marks + save-to-disk
- [x] Automated layout assertions (27, Vitest + viewer badge): connectivity, two disjoint routes to every zone hub, per-door alternate-route rule, air rule (Dijkstra at swim speed, 45 s shallow / 75 s deep), dead-vertical chain ≥25 m, counts (squeezes/dead ends/mounds/tapes/toys/posters/perks/doors/burrows)
- **DoD met:** 27/27 assertions pass; blueprints match DESIGN §5 zone-by-zone (ring + spurs, lattice + dead ends, vertical bore with chimney alternate, abyss cluster) and are archived at `docs/layout/blueprint-{top,side}.png`; game mode regression-checked after the `?view=map` branch refactor.

### M2 — Geometry & collision ✅ 2026-07-18
- [x] Cave mesh from the graph via SDF (capsules + spheres + noise) extracted with **surface nets** (simpler than marching cubes, no case tables, watertight) — 60k tris in ~210 ms; zone palettes + noise shading as vertex colors, flat-shaded (triplanar shader unnecessary at our fidelity)
- [x] Closed-door blockers: debris/grate/hatch meshes from edge data + SDF plug spheres (CSG); debug buttons open any/all doors
- [x] Collision = analytic SDF push-out (same field as the mesh, so they can never disagree); squeeze forced-slow via region detection (no-turn-around + camera pull-in deferred to the M3 body controller — freefly has no body)
- [x] Water surface + above/below environment switch; **sky shaft added to data** (the cenote must be open-air per LORE §3 — also gives the from-below light beacon); debug teleport-to-node + live region/depth readout
- **DoD met:** SDF test suite proves every node/edge midpoint passable, all door plugs seal and un-seal, and free alternates survive all-doors-closed; in-browser traversal verified through the main door (blocked → opened) and the squeeze crack (3 m/s slow, region-detected); interior screenshots from all five zones show sealed, readable rock; 0.03 ms/frame.

### M3 — Player: swim, air, light ✅ 2026-07-18
- [x] 6DOF swim: momentum verified to spec (2.56 m/s at 0.4 s vs 2.52 theory; exact glide decay), camera-relative Space/C verified (looking straight down, Space moves you 2.77 m horizontal / 0.06 vertical), ambient current drifts idle player ~0.27 m/s; squeeze speed clamp + FOV pull-in; **walk mode** on dry land with gravity, landing, and wade-in/out transitions
- [x] Air system: drain × sprint × zone verified, pocket/surface refill, low-air vignette, drowning damage (HP 100→22 in the verification run), regen after delay, death overlay + R restart
- [x] Flashlight: F toggle, dim curve, flicker below 20% (verified two-level flicker), battery drain; bubbles: round soft sprites exhaled from the mouth, rising world-up
- [x] HUD v1: O2 bar + number, battery pips, depth gauge + trend arrow, points/ammo placeholders, death screen in ledger voice
- [x] Debug: god / infinite air / infinite battery / air & battery sliders / damage button; harness exposes vitals + player
- **DoD met:** full drown→recover-at-pocket loop executed end to end via harness (air 2 → 0 → HP 22 → pocket refill → regen to 100); flicker verified varying; depth reads correctly aimed straight down; 0.06 ms/frame; unit tests for all vitals math (5 new tests, 40 total).

### M4 — Atmosphere & impairment ✅ 2026-07-18
- [x] Fog/darkness grades per zone (FogExp2 density = fogK/visibility, per-zone palette §15); ambient light falloff with depth; sinkhole god-ray billboards; ambient particulate (camera-following mote box, cyan-tinted in the deep); close-wall headlamp exposure (cone-probed, quadratic — resolves the M2.5 blowout debt)
- [x] Ambient silt: silty-floor stir on fast/low swimming or sprinting (SDF floor probe), vis → 12 m, 20 s settle; camera-local silt particle cloud (fog carries the vis collapse, particles sell the texture)
- [x] Chalk mounds: 6 placed by SDF floor probe at chalkMound nodes, stacked-bulb mesh + armed shimmer, slump when blown; TOUCH detonation → chamber silt-out (vis 4 m, beam narrows + throw 22 m), 75 s fade, re-arm on full clear (shot detonation wires up with weapons in M5/M6 via `silt.detonate`)
- [x] Tilt zones per edge tags: roll drifts 15°/s with wandering sign, per-zone caps (edge regions + interior bore junctions), 2°/s decay outside, X re-levels 45°/s; camera-relative strafe under roll; max-tilt accessibility slider persisted via settings stub (`ui/settings.ts`)
- [x] Guide line: anchor/pin at tieOff nodes (Q), ~1 m pay-out points, reel-meter conservation, re-reel by walking the free end back, follow mode (hold T) glides 3.5 m/s at any visibility; chemlights (G): toss, SDF sink-and-rest, run persistence, cap 40, pooled real lights on nearest 6
- [x] Debug: silt-out/stir/clear buttons, roll + max-tilt sliders, fog-off toggle, give reel/chemlights; HUD kit readout (REEL/OUT/GLO, ON LINE state)
- **DoD met:** screenshot set `docs/screens/m4-chapel-{clear,stir,siltout}.png` (vis 26 → 14 → 4 in the White Chapel); fade sequence `m4-fade-{25s,50s,rearmed}.png` with mound re-arm asserted; scripted run laid 87 m of line mz-gate→chapel with 5 chemlights, touch-detonated the chapel mound, and followed the line back BLIND to the Maze hub (arrival 2.5 m from center, on-line every steering frame — `m4-follow-blind.png` shows the line as the only readable thing at vis 4); inverted 180° in the bore with the exhale cluster marching screen-down across `m4-tilt-seq-{1,2,3}.png` and X re-levelling 180→0; 65 tests pass; 0.02–0.3 ms/frame with silt-out + particles live.

### M4.8–M4.9 — THE LEVEL EDITOR ✅ 2026-07-19 (unplanned; user-requested mid-M4)
Not in the original plan — added when the user asked for "a tool for me to easily design levels" and it became the map workflow. Visual editor at `?edit=1` (`src/editor/`): orbit/select/drag rooms, tunnels, waypoints; gizmo tools move / R orient (falseUp, water tilts live) / W water level; shift-click connects tunnels, dbl-click adds waypoints, waypoint→turnaround-room; live DESIGN §5 rule checks; ⛰ real-rock preview; 💾 saves `layout.json` via dev middleware; ▶ TEST plays the unsaved layout (noclip at the editor camera, F4 returns); every TUNING knob editable with persistent overrides. Layout migrated to world-unit `layout.json` (single source of truth) and the water model went relative (`water` fraction, `airGap`) to fit the editor's manipulators. Details: M4.8/M4.9/M4.9b worklog entries; manual: docs/MAPPING.md.

### M5 — Zombies & rounds ✅ 2026-07-19
- [x] Drowned: procedural drift-swimmer box-rig, 3 variants (recurrence is canon, DESIGN §8.1), graph A* + polyline steering, grab (35 dmg, −8 air, roll kick + shove; firm unhurried windup), corpse drift (current + slow settle + fade), workstation-pause idle at facility-tagged nodes
- [x] Burrow spawning (score = distance + in-sight penalty + off-zone penalty, ≥12 m, per-burrow activation rounds; grounded-crawl haul-out so the surface decays on schedule §5.1)
- [x] Round system (`zombies/rounds.ts`, pure + unit-tested): counts/HP/speed curves from `tuning.ts`, 40 s timed intermission, 8 s first-round grace, the Cave Stirs countdown (scaled threshold, 45 s, survivors carry over), visual tally flicker stinger (audio stinger lands with M8 per §14)
- [x] Hit flash + stagger, deaths, points 10/60/100/130 with HUD +ticks; Wrist Dart (hitscan + tracer v1 — distinct-feel pass is M6's) + Dive Knife on RMB/V; dart shots detonate chalk columns (closes the M4 `silt.detonate` debt)
- [x] Debug: spawn 1/5 at node, start round N, kill all / kill-all-but-2, rounds-pause toggle (auto-paused in `?playtest=1`), round/phase/alive in the status line
- **DoD met (scripted observation in place of twitch play, per the M9 honesty rule):** 10/10 chase spot-checks arrive across all five zones — vertical bore included — max 1 s without progress; both closed-door free alternates used (A* excludes closed doors, unit-tested; SDF plugs are the physical backstop); Cave Stirs E2E: round 1 culled to 2 stragglers → visible countdown → round 2 began regardless, survivors carried against the alive cap; grab verified 100→65 HP, −8 air, +22° roll; shore player reached in 10 s (surface decay); round-6 observation: three burrows converge, 9-cap full at 15 s, 264 HP each — the shore economy collapses on schedule while rounds 1–5 stay holdable; 9 zombies ≈ 0.09 ms/frame (fresh-page bench); 99 tests pass (+21 this milestone).

### M6a — Economy core ✅ 2026-07-20 (split 2026-07-19 per the scope-creep risk note: M6 was two sessions of work)
- [x] Interact system: context-sensitive E (M4.7 note honored — E rolls the camera EXCEPT while an interact prompt is live; verified 0° drift with prompt, 34° without), tap for buys, hold for doors/power, prompt + progress UI
- [x] Doors: buy-open flow (cost, crumble-fade grind + silt puff, permanently open, zombie pathing unblocks live — chase 26 s → 11 s through the bought artery)
- [x] Wall buys (5 wall guns; battery/chemlights/reel vendors with the 400 m reel cap; ammo at half cost); steel-locker + chalk-slate visuals, SDF wall-mounted
- [x] Arsenal: slot system (+3rd with Deep Pockets), 1/2/3 + wheel switching, buy/replace/refill rules, auto-fire on hold
- [x] Power (the Pile's breaker, hold-E): 39 cherenkov bulbs trace the two arteries, stations vend; powerGate grind is generic — **the user's current map carries no powerGate edge** (pap-chamber was rewired to throat-rim-air in their authoring round; binds the moment one exists)
- [x] Perk stations + 4-cap + all 9 effects live-verified (maxHp 220 + heal, air 150 + drain, speed ×1.15, tilt decay ×3, vis 35→49 m, beam ×1.25, reload/fire mults, slots 3, noStir); Second Wind full flow (blackout w/ death-screen suppression → wake at last-used pocket with sidearm → consumed, re-buyable at the station)
- [x] Debug: power toggle, give perk (select/all/clear), give weapon, +20000 points
- **DoD met:** one-session sweep bought 6/6 doors, 5/5 guns + 5/5 half-cost refills, 3/3 consumables (cap refusal proven), 9/9 perks across three 4-cap batches — 39,000 points through the economy; guns mechanically distinct (speargun 130 single · Pneu 10 rds/s held · Flechette 6-of-8 pellets one trigger · Harpoon one bolt through 3 bodies · Lance twin-kill stab, no ammo); Second Wind saved and spent itself; affordability by simulation: the power path clears with 2× margin by round 3, 3 perks + SMG + two doors inside round-9 income (unit-tested against live tuning); 0.13–0.22 ms/frame with all shops built.

### M6b — Arsenal & drops ✅ 2026-07-20
- [x] Requisition Roulette at A/B/C: crates at every spot, ONE live (warm glow through the slats), spin → billboarded requisition card cycles → 8 s take window → sinks back if lapsed; tease-and-move (free spins, then chance + guaranteed-by-12) leaves the wind-up toy diver in the cold crate and refunds the fee
- [x] Box guns, each mechanically distinct: Twinfish (2-ray burst, 1-hand on the last spear), Arc Projector (chains 4 with ×0.7 falloff along real tracer links), Vortex Maw (4/4 caught, crowd spread 1.27 → 0.02 m), Sonic Lance (4-body line pierce), Bang Stick (one-hit shell — killed a 6,762 HP round-30 body — shell-per-shot reload, upgrades RMB melee while owned)
- [x] Feel pass v1: per-gun tracer colors + lifetimes (zippy pneu 0.08 s ↔ heavy harpoon/sonic 0.3+ s), raise-beat on switch/buy (carried-delay bug fixed + regression test); deeper feel judgment stays with M9's manual passes
- [x] The Bench: power-gated machine (the user's map has no powerGate edge — the MACHINE stays cold until the Pile is live; same intent, no map edit), 5000 → 3 s work → rename + ×2.5 dmg + ×1.5 mag/reserve + per-gun quirk for all 11 guns; wall lockers vend PaP ammo at 4500; the universal rule verified lamp-off in the dark maze (impact light 12.7 intensity, screenshot)
- [x] Drops with pity: all 6 verified (full refill · +120 on 60 · one-dart kill on 1,292 HP · silt-out cleared + mound re-armed · battery 0.15→1 · 4→0 alive +400, stacked ×2 with Double Points) + world-pickup swim-over + pity-forced spawn at 41 kills
- [x] Debug: PaP-free button, force box move, spawn/force any drop, box guns in the give-select
- **DoD met:** every box gun + a PaP version bought and fired in one debug session; the papped bolt visibly lights a dark tunnel; tease-and-move observed with refund + relocation; all 6 drops triggered with effects verified numerically; 0.19–0.25 ms/frame; 118/119 tests (the 1 is the standing user-map teaser item).

### M7 — Specials, Abyss, win/lose ✅ 2026-07-20
- [x] Angler: chemlight-green lure on a stalk (bobbing, pooled light), lunge on approach (verified 100→55 exactly), relocates after strikes, battery drop on kill (+200), round-start spawn roll (r8+, dark zones); Silt Shade: forms 3 s into the player's silt-out, hunts inside its cloud only, dies the moment the silt settles
- [x] Guardians at both posts: patrol orbit, three sensory tiers (10 m lights-off sneak for 10 s = ignored; lamp on = aggro in 1 s), heavy hit (60 + shove + −6 air rip through the real funnel), killable (500) but the suit walks its post again next round; Cathedral biolum field (~900 seeded points) makes lights-off navigation read
- [x] The Heart at the apse (pulsing, lit) → hold-E grab → the Ascent: rounds superseded, global burrow spawning on a flat 3.5 s clock, zombie speed hard-capped ×0.76, vis one grade down, the carried glow is a real light; surfacing into daylight with it = win. Second Wind death drops it where you died (settled to rock, re-grabbable, ascent persists)
- [x] Win + death screens in Lowe's ledger (RECOVERED/ROSTER 41/DISCREPANCY/SHIFTS/TIME UNDER/DRAUGHTS/SPENT), the win footer lands the forty-two beat; specials hittable by every weapon path (ray absorb + melee arc)
- [x] G13: the photograph pinned at the drill head — procedural print (the date IS the wrongness), E → fullscreen inspect overlay with real-text caption, E closes (the M8 poster-inspect pattern, landed early)
- [x] Debug: spawn Angler at node, kill specials, remote heart grab, win-screen preview
- **DoD met:** every special script-verified (ambush/silt-bound death/sneak-vs-lamp/respawn); the full assisted run (points + transit assists logged, NO god, real air) became its own story — the unarmed attempt DIED in the chimney (the race is real), the armed run grabbed at −171 m, died twice to the ascent swarm with the Heart dropping and re-grabbing both times under fire, recovered 44 on the way up, and surfaced to a win screen whose ledger matched the harness digit for digit (44/41/+3 · 4:32 · 3 draughts · 8,500 spent); 118/119 tests.

### M8a — Audio engine & synth soundscape ✅ 2026-07-20 (split 2026-07-20: M8 was two sessions — engine/wiring here, generated content in M8b)
- [x] WebAudio graph (`src/audio/engine.ts`): underwater bus (850 Hz low-pass + procedural convolver) ↔ surface bus crossfade on head-above, silt-out cottony muffle (→450 Hz), positional voices (distance gain + stereo pan + SDF-ray occlusion: 420 Hz + ×0.35 through rock), all knobs in `tuning.audio`
- [x] Full soundscape synthesized in WebAudio (`src/audio/sfx.ts`, 29 voices — also M8b's per-sound fallbacks): breathing tied to HR (period = 60/HR, faster in reserve), low-air heartbeat at the real HR, moans (honest position per §13), Angler hum (faint wrong dyad), Guardian sub-bass presence, silt whump + tinnitus ring, round/Cave Stirs stingers, perk jingle, box music-box tease, PaP choir-groan, geiger near the Pile (Poisson, flavor only), door grind, 9 per-gun-family shots (+PaP zing), knife, grab, drop chime, drown pulse, death/win stings
- [x] AudioDirector (`src/audio/director.ts`): one snapshot per tick + explicit event hooks in main; `recent[]` event log (verification now, M9 pilot logs later); master volume slider persisted in settings; debug Audio section triggers everything
- **DoD met (instrumentation — I can't hear):** all 29 voices render nonzero RMS in an OfflineAudioContext; surface vs underwater brightness 1.415 → 0.116, full muffle → 0.059; positional gain 0.9 at 2 m → 0.038 at 30 m, pan ±1 correct, occlusion 1 through rock / 0 in open water; live-director E2E via offline engine: breath cadence, moans with per-body gain/pan, heartbeat at air 12, drown pulses at 0, round stinger exactly on the 2nd+ round change, stirs swell, angler loop attach/detach on spawn/kill, 37 geiger ticks at the Pile, door/box/pap edges, death & win stings; 0.29 ms/frame with the director live; 118/119 tests. Real-speaker path (context resume on the play click) is standard-pattern; user headphone pass invited.

### M8b — Voices & flavor audio ✅ 2026-07-20 (split 2026-07-20 again — images/job-sheet/menus moved to M8c; **generation itself BLOCKED: both API keys in `.env` are invalid**, so this milestone ships the complete runtime systems on their mandated fallbacks + a ready-to-run generation script)
- [x] Radio logs: 6 tape props (olive reel-to-reel + REC bead, SDF-floor-rested) at the `tape` spots; pickup toast, auto-play at next safe surfacing (no enemy ≤20 m), pause-on-dive with held position, B-skip; typewriter subtitles from the LORE §5 scripts; audio from `public/audio/` when generated, radio-crackle + subtitle-clock fallback until then (replay-from-pause lands with M8c's pause menu)
- [x] Lowe VO (`src/audio/{lines.json,lines.ts,voice.ts}` — line data is ONE json both the game and the generator read): surface-only VoiceQueue with the full LORE §2.1 anti-spam (120 s ambient cooldown, once-per-run, tapeReact>event>ambient, 40% roll, silence default); all 32 lines wired — surfacing/pocket/close-call rotation, tally 100/300, round + stirs heard from surface, power, tape reactions, toys, jukebox, Second Wind wake, idle, the win beat
- [x] Easter egg: 3 tin divers (shimmer loops ≤8 m via positional music-box synth, stop when wound) → jukebox prop wakes, plays a random track from `public/music/easteregg/` through the underwater bus; folder auto-listed (dev middleware + build snapshot) so user-dropped MP3s need zero code; placeholder music-box WAV committed
- [x] `scripts/generate-vo.mjs`: casting (LOWE_VOICE_ID pin or heuristic), 32 Lowe lines + 6 tapes at distinct voices + Eleven Music attempt, writes `manifest.json` that flips the runtime off fallbacks; idempotent, `--dry`, friendly invalid-key exit
- [x] VO/music volume sliders + subtitles toggle persisted; debug: collect-all-tapes, wind-all-toys, skip, queue-state readout
- **DoD met:** harness E2E — T3 picked up underwater at the Pile, played at the shore (subtitle typewriter 126→152 chars, `B skip` hint), paused on dive with reel position held, resumed on surfacing, skip fired Lowe's reaction (`LOWE` subtitle); T1 deferred 5 s+ while a zombie stood inside 20 m and rolled the moment it died; toys 3/3 wound → jukebox chose `placeholder-music-box.wav` from the live folder listing; close-call line queued surfacing at 6 air; forced-queued events held (queue non-empty, `voice.current` null, subtitle hidden) through 10 s submerged; 11 new unit tests pin the §2.1 rules; generation script exits 2 at the auth wall; fresh-page bench 0.35–0.5 ms; 129/130 tests.

### M8c — Images, job sheet, menus ✅ 2026-07-20
- [x] All 21 LORE §7 images live as **procedural canvas prints** (`src/game/media.ts`: aged paper, stencil type, period pictograms — and the text is LEGIBLE, which AI lettering never reliably is); `scripts/generate-images.mjs` committed ready (**Gemini image quota is 0 on this key — needs billing; one command upgrades every spot, zero code**); posters + MISSING notice + blueprint + crew photo + rec sign placed at their `poster` spots (`src/game/posters.ts`, wall-mounted, `exclude`-dodging co-located fixtures), 9 individual perk-label plaques beside their stations, patch + title art in the menus, G13 shared between the job sheet and the drill-head print
- [x] **Inspect overlay** on every poster (look + E → fullscreen + real-text caption; E toggle press-swallowed so close can't instantly reopen — M8c fix)
- [x] Job-sheet intro card at run start: LORE §2.3 verbatim on aged paper with the impossible 1971 enclosure as a real thumbnail; E/click signs it
- [x] Menus (`src/ui/menus.ts`): title (key art + patch + DIVE/HOW TO DIVE/SETTINGS) → job sheet → play; Esc/pointer-lock loss → pause (RESUME / recovered-tapes replay list / SETTINGS / HOW TO DIVE / RESTART); settings per DESIGN §12 incl. new mouse sens, invert-Y, FOV (squeeze pull-in now relative), brightness (canvas filter), all volumes, subtitles; How-to-Dive in exactly 10 lines; menus freeze the run (loop-gated tick; harness unaffected)
- **DoD met:** job sheet card verified on run start (headline, pencil note, thumbnail) and E-signed away; G5 inspect opened with its real-text caption, closed, stayed closed while held, reopened on a fresh press; poster screenshotted in-world readable by flashlight (`m8c-poster-pile.png`) 7.3 m clear of the breaker it used to overlap; **every image on screen IS the fallback** (no generated files exist — stronger than the delete-one-image proof); pause listed and replayed a recovered tape; world verified frozen while any menu is open; FOV 92 and brightness 1.3 applied live from the settings sliders; 129/129 tests; console clean.

### M8d — User round: death vignette, REMORA, music everywhere, gallery, editor audio UX ✅ 2026-07-20
- [x] **BO-style death indicator:** `#hud-blood` — blood blooms in from the screen edges below ~2/3 HP (corner splatter + edge bloom, quadratic ramp), pulses (`.critical`) under 30% — verified opacity 0.48 at 22 HP with the pulse class live
- [x] **Music bus** (`engine.music`): music no longer rides the ducking underwater bus — constant level, water only changes the TONE (low-pass 1100 Hz submerged ↔ open 20 kHz in air). Fixes "music very quiet in open air". Jukebox + lull route here
- [x] **The jukebox is a fixture:** E-interact — dead it hints at the sleeping divers (the egg stands), woken it plays/advances through the folder (shuffled once, then cycles, NOW PLAYING toast); **3 new period songs generated** (Moonlight at the Waterline · The Cormorant Twist · Forty-One, Forty-Two — one quiet wrongness each) joining Still on Shift
- [x] **Menu music** (`/music/menu-theme.mp3`, generated): loops under title/pause/settings, plain element outside the frozen run, autoplay-block retried on first gesture
- [x] **The lull** (`/music/lull.mp3`, generated 1-min calm-but-sinister piece): plays on the music bus after `audio.lullAfterSec` (240 s) with no dialog (tape/Lowe/REMORA), `lullCooldownSec` 600 s + 150 s opening grace; refuses when dead/won — verified firing in a 158 s no-dialog fast-forward and refusing on a dead player
- [x] **The REMORA unit** (LORE §2.4 added): client-supplied Type R dive monitor, speaks ONLY underwater (Lowe's exact complement) through the helmet speaker (master bus — water never filters her); 22 lines in `lines.json` — depth 50/100/150, low air, reserve, lamp cell, silt-out, first contact, Angler/Guardian, the item's warmth, the Ascent clause, power, stirs, abyss, 6 ambient musings; same anti-spam queue as Lowe; subtitled REMORA; all 22 generated (voice: Sarah) — verified hello + 6 lines queued at −170 m and silence on surfacing
- [x] **Photograph gallery:** every inspected print/poster/photo files itself (`game/gallery.ts`) into a pause-menu PHOTOGRAPHS grid with a click-to-view lightbox; the three tin divers add procedural polaroids when wound (`media.toyPhotoDataUrl`, one wrongness each) — verified 3 polaroids after windAll
- [x] **Editor audio-node UX:** hidden by default (♪ toolbar toggle; +♪ auto-shows), fat 2.4 m invisible pickers, filter now makes non-matching nodes/tunnels UNCLICKABLE (raycast disabled, not just ghosted), zone SHAPE via the same `stretch` field rooms use (shells + runtime ellipsoid distance with honest pan via virtual-source projection)
- [x] **Bug fixes:** editor ▶ TEST click-eating overlay (`#menus` now starts hidden — the un-rendered menu root was a fullscreen wash), H frees/re-locks the mouse for the debug panel (without triggering pause-on-unlock), zombies in falseUp air rooms now take gravity/grounding along the LIE (verified falling along −Z in the 90° Listing Room), scarier regenerated ambient-mid/deep beds + hotter deep-tier gains (×1.2/×1.45), generate-vo no longer wipes the manifest's `sfx` section
- **DoD met:** 129/129 tests, tsc clean, console clean; playtest overlay verified gone (menus hidden, `blocking:false`, noclip live); every trigger above driven in-browser via the harness; `m8d-audio-node-ux.png` archived.

> **M9 (balance) and M10 (ship) were removed from this plan 2026-07-20 at the user's request — those passes are the user's to drive by hand.** The pilot-mode/simulation notes live in git history (commit 8366d03 and earlier) if ever wanted again.

### M11 — Docs reopened + audio quick fixes ✅ 2026-07-21 (the user's redesign round, part 1)
- [x] DESIGN.md rewritten where the user redesigned: §2 pillar 2, §4 arc, §8.2 Angler rework + §8.5 Lamp Man, §9 shifts + wandering population, §10 found-item economy, §12.1 Museum Annex + Concept Gallery, §14 in-head VO / arbiters / Moonlight ending / shift bell / party track
- [x] LORE v3: lock lifted for **expansion only** — voids #9 (the lamp) + #10 (the bells) added, spread-the-wrongness rule (no new count material), in-head VO doctrine (§2.1), REMORA is-she-real doctrine (§2.4), 14 new Lowe + 7 new REMORA lines (§2.2.1), item identities (dynamite/keys/slugs/bell/Lamp Man/Annex/morale button §4), "Morale Night" party song spec + lyrics (§6.1), concept-art manifest C1–C12 (§7.1), jukebox always-anthem-first rule (§6)
- [x] CANON.md created: everything built ↔ lore trace (rooms, enemies, objects, all 60 voice lines + 6 tapes, 21 images, music, SFX), with explained/wrongness/pure-gameplay classification + standing upkeep rules
- [x] **Sample auto-gain:** SampleBank peak-normalizes every decoded sample (boost-only, ×16 cap) — root cause of the user's unheard box/PaP/shift sounds was quiet generated masters (flagged since the generation round)
- [x] **amb-machinery regenerated** as one enormous slow distant pump through rock (user: "sounds really weird")
- [x] **Still on Shift always plays first** when the toys wake the jukebox (folder still shuffles behind it)
- [x] Gemini image quota re-probed: still hard 0 — billing remains the blocker for all image work
- **DoD:** docs internally consistent (design ↔ lore ↔ canon cross-references verified); tests + tsc green; normalized stinger/box/PaP RMS verified through the offline engine; machinery loop decodes and loops.

### M12 — One voice, one song (audio direction round) ✅ 2026-07-21
- [x] **One speech slot** (`audio/speech.ts` arbitrate): tapes block both voices; an active speaker is never talked over; free slot goes to the better head-of-queue category (ties → Lowe — it's his head); wired through both real queues in main; 4 unit tests + a 900-frame live drive with ZERO simultaneous frames
- [x] **MusicDirector** (`audio/music.ts`, ONE slot): jukebox/lull/Moonlight (+menu theme yielding, party at M16) — starting any song stops the current; **the lull only grows from true quiet (no music AND no dialog for `lullAfterSec`)** and yields instantly; jukebox+lull collision dead; 5 unit tests; menus theme now pauses whenever a game song holds the slot and resumes via `onStopped`
- [x] **In-head Lowe:** the 3 s surface gate is GONE — lines play anywhere alive (LORE §2.1 v3), through a dedicated head chain (low-pass 4.8 kHz warmth + compressor + 28 ms whisper double, straight to master — never water-filtered); measured: 100 Hz/1 kHz pass full, 8 kHz damped ×0.45
- [x] **Robotic REMORA:** telephone band (320–3300 Hz) + 52 Hz ring-mod tremolo + hard compression; measured: 100 Hz crushed ×0.17, 28% amplitude wobble — unmistakably a machine, still in his head
- [x] **v3 line batch:** all 20 LORE §2.2.1 lines generated (Bill/Sarah, idempotent run) + wired: swim musings offered every 100 s submerged, bell.1/rem.bell.1 on submerged shift change, heart.near/rem.warm proximity, heart.carry + rem.stirs re-pointed into the Ascent, client.1 joins late-run idle, rem.works.1 on gun pickup, rem.silence.1 after 300 s true quiet; lamp/museum/party/hatch lines generated + dormant until M15/M16/M13; Lowe's stirs.1 re-pointed to crowd pressure (≥7 alive)
- [x] **Moonlight ascent finale:** carrying the Heart shallower than 50 m with no song playing auto-starts it (live-verified via harness ascent); the win **holds inside the song** — `engine.muteWorld()` fades the world buses, music + head voices stay, the winSting skips when a song is playing, win.1 lands in-head over the coda
- [x] **Shift bell generated** (4 s brass toll, ×1.4 normalized, synth fallback) + `bellSequence(5)` ready for M13's hatch; debug buttons for both
- [x] Debug: speech/music state readout, Moonlight-now, stop-music, swim-line offer, bell ×1/×5; `bw.music` on the harness
- **DoD met:** 138/138 tests (9 new arbiter tests); chains measured through offline renders; live harness drive: 0 voice overlaps in 900 frames, moonlight→jukebox slot handoff in order, anthem still first; console clean; tsc clean.

### M13a — The free cave: economy core ✅ 2026-07-21 (M13 split mid-session per protocol — the editor half is M13b)
- [x] Points → **the ledger** (vanity): HUD reads `LEDGER n`, ZERO spend paths anywhere in the codebase (Points.spend has no callers), death/win ledger drops SPENT and gains the zero-kill beat (`DISCREPANCY none. first time for everything`)
- [x] Lockers/shelves → **free issue on the one-per-bell cooldown** (`BellIssue` per station, `shops.issues[]`); chalk slates read stock (`IN STOCK` / `ONE PER BELL`), guns free first take, ammo (incl. PaP ammo) per-bell; Requisition Roulette free per bell — and **the tease refunds your bell** (pullBell.reset), the site owes you one
- [x] **Draught flasks:** free with the Pile live (`TAKE THE FLASK — DRINK IT DOWN`), 4-cap + Second Wind refill-after-use untouched (they always lived in Perks.vendState)
- [x] **Pickups in data** (`contents.pickup`, schema in data.ts): 4 dynamite (sink-crack at the crack mouth, gal-spur-battery, gal-turn, mz-d2) · 2 tagged brass keys on hook boards with more hooks than keys (gal-rec → PILE GRATE, mz-infirm → STORES GRATE) · 3 fuel slugs (gal-pile, mz-d5, drill-head); world props SDF-rested (`economy/pickups.ts`), belt HUD line (`DYN ×2 · KEY: … · SLUG ×1`); debris consumes a charge, grates check the ring (keys are kept — a used key is a story)
- [x] **The Abyss hatch tolls five bells:** free to crank → `bellSequence` ×5 + round counter +5 (`rounds.startRound(n+5)`) with the director's horn synced away so the bells own the moment + `bell.2`/`rem.hatch.1` queued + toast
- [x] **The Bench:** power + exactly one slug per upgrade (`FEED THE BENCH · 1 SLUG`); Angler's rare slug drop deferred to M15 (its rework session)
- [x] Tests: affordability sims DELETED → **availability sims** (`economy/availability.test.ts`: zero-kill no-squeeze fixpoint reaches power, all 9 racks, the Bench, the Heart on found items alone; pickup-less run still un-softlocked via free alternates; data integrity: every grate has its key) + **headless free-cave E2E** (`economy/freecave.test.ts`: the full pickup→door→hatch→locker→box→Bench flow through real constructors, no browser needed)
- [x] Debug: give dynamite/keys/slug, reset bell issues, hatch-toll button; harness exposes `inventory` + `hatchToll`
- [x] **Browser smoke owed — PAID at M13b start:** full in-browser drive of the real E-key flow (crate taken at the crack mouth → belt `DYN ×1` → choke blasted+consumed → key taken → grate unlocked with its tag in the prompt → slug taken → hatch cranked: rounds 1→6, five-bell toast); all pickup positions SDF-verified in open water; prop + price-less label screenshots archived (`m13b-pickup-dynamite.png`, `m13b-pickup-keyboard.png`)
- **DoD (headless first, browser confirmed at M13b):** 153/153 tests (9 new integration + 7 new availability), tsc clean, production build clean; every economy flow driven through the real constructors and interact prompts in node, then re-driven in the live game.

### M13b — The free cave: editor round ✅ 2026-07-21
- [x] **Editor: tunnel view** — ⬭ toolbar toggle: every tunnel as translucent per-segment tubes at its REAL width-class radius (squeezes visibly thinner, knee spheres at bends), door/gate rings at their true arc positions colored by kind + ⛔/⚡ labels; view-only meshes never steal clicks (raycast disabled — the invisible-picker lesson applied preemptively); `m13b-tunnel-view.png`
- [x] **Pickup placement UI:** node panel `pickup` dropdown (none/dynamite/key/slug); key mode reveals a `keyFor` dropdown listing every real door id + a tag-label field; node labels gain 🧨/🔑/⚛ glyphs so the economy reads at a glance; round-trip verified in-browser (flip to key → keyFor options correct → restored to dynamite)
- [x] The M13a browser smoke (checked above) — full free-cave flow driven live
- [x] Label polish: locker slates now read `FREE ISSUE` (matching their prompt), vendors `ONE PER BELL`, draughts `DRAUGHT` — verified in-world by screenshot (IRON LUNGS rack)
- **DoD met:** all 28 editor rule checks green with the new views active; tunnel-view screenshot archived; pickup form round-trip proven without dirtying the user's layout; 153/153 tests, tsc clean.

### M14 — The living cave ✅ 2026-07-21 (time-based shifts + wandering population; DESIGN §9)
- [x] **Shift clock:** pure time (`ShiftSystem`, rounds.ts rewritten): shiftSec 90 s, the WATCH BELL rings every change (the somber horn retired), HP/speed curves keyed to shift, mob cap = `4 + 1.5×shift` hard-capped 24; intermissions + the Cave Stirs DELETED with their tests (a 3-shift zero-kill clock run pins time-only advancement)
- [x] **Map-wide pack spawning:** UNIFORM burrow pick (bias funnels everything through the nearest — observed 16/16 hunters before the fix) with a floor just above aggro range (without it, despawn-respawn cycles RATCHET the cave into hunters camped on the player — observed 23/24); packs of 1–5 emerge one-by-one from the same proven point on a stagger (paranoia honored by never inventing placement math — DESIGN §9 wording updated); Ascent spawner keeps near-spawns
- [x] **Wander mode:** far Drowned drift the squeeze-free graph between non-burrow, non-leaf nodes (verified: 23/23 wandering across maze/galleries/abyss/throat, 22 moving with targets); escape leg from a squeeze-only burrow may use any open edge (observed 8/9 idle without it); aggro at 25 m or 38 m with LOS (verified live wander→hunt), deaggro back after 6 s beyond 48 m; workstation pauses survive on wanderers
- [x] **Despawn/respawn:** far + unseen roll every 4 s at 12% (verified 12 recycled in 2 min with the cap holding); never near, never in sight; population refills elsewhere — the "swim fast, see nothing" failure is closed
- [x] **Perf LOD:** far wanderers animate at ~8 Hz and skip the separation solve — fresh-page bench: 24 alive costs 0.64 ms/frame (baseline noise 0.79)
- [x] **Zero-kill run proven live:** assisted transit (teleports+god logged), real systems — Heart grabbed, Ascent, surfaced, WON at `RECOVERED 0` with the ledger's new beat: `DISCREPANCY ... none. first time for everything`
- [x] Debug: despawn-roll toggle, shift-clock pause, hunting-count in the status line (`S8 bell in 42s · 16 alive (3 hunting)`); spawn-N-at-node + start-shift-N carried over

### M14.5 — The Roster of 41 ✅ 2026-07-21 (DESIGN §8.6, user idea — every Drowned is a person)
- [x] **The crew book** (`src/zombies/roster.ts`, internal, never player-visible): 41 profiles — names seeded verbatim from T6's read (Albrecht/forward watch, Ames/galley, Barrow/lamps, Bell/stores, Calloway+Carver/drill, Deem cut off mid-name) + 34 more across the site's departments; watch-bill weight, speed/HP mults (±15%), rig seed, fixed moan sample+pitch, optional quirk — ALL deterministic (mulberry32 per man; `buildCrew()` deep-equal across builds, unit-pinned)
- [x] **One of each, ever:** spawn tickets `Roster.checkout()` (weighted pick over the men below); despawn/recycle returns the man; **a kill holds his slot until the corpse settles off the water** (remove → return) — proven live: Bell's drifting corpse refused his own respawn, then walked again after the fade
- [x] **Bespoke rigs:** per-man build (scale/shoulders/limb length), seeded palette jitter over 3 workwear kits, exactly ONE role prop each (Barrow's chest dry-cell+lamp, Bell's hip satchel, Marsh's back canister, drill hard hats, watch caps, aprons, kit boxes…) — `m145-carriers-lineup.png` shows three men apart at a glance
- [x] **Personal equipment drops the lore-safe way:** carrier ALWAYS drops (Barrow→CELL, Bell→MAX AMMO, Marsh→a new `fuelSlug` world drop feeding the belt, excluded from the random roll); balance via watch-bill weights (0.35/0.3/0.12 vs 1.0 — observed 0–1 carrier watches vs 3 for regulars over 60 spawn events); battery shelf KEPT pending the user's play verdict
- [x] Per-man moan identity (fixed sample + 0.78–1.22 rate through SampleBank + synth fallback; director logs `moan <name>`); **Lowe's surface.5 "Barrow, was it" now gated on Barrow having walked 3 watches** (verified shut at 2, open at 3)
- [x] LORE §4 Drowned row rewritten (few-variants doctrine superseded), CANON rows added (roster, personal drops, gated Barrow line); the Lamp Man holds no row — void #9 untouched, and the uniqueness test pins it
- [x] Quirks live: pauser (×4 station stops), runner (never pauses, +10% pace), lingerer (stands at the burrow mouth ~8 s — verified pausing with negative stateT after emerge)
- [x] Debug: Roster section — 41-man dropdown (role/⚑carry/quirk), spawn-crewman-at-node (duplicate refused), who's-on-watch + watch-count readouts, per-man weight-override slider + clear; harness `bw.roster`/`bw.crew`
- [x] Tests: 10 new (uniqueness incl. 42nd-checkout-null, determinism, T6 canon rows, stat/voice bounds, carry mapping, watch-bill balance, override-to-zero strikes a man) — 160/160 total
- **DoD met (browser, harness-driven):** population filled shift 8→9 with 16 unique named men all wandering; 60 spawn events over ~2 min churn produced zero duplicates and 36 distinct men walked; carrier kills through the real shot funnel dropped CELL/AMMO/SLUG at the corpses (battery 0.2→1.0, reserve refilled, belt slugs 0→1); named moans in the director log; console clean; tsc + 160 tests green. **Perf honesty note:** the pane-hidden 0×0 canvas makes render benches noise (M14's 0.64 ms included) — at a forced 1280×720 on the real Intel UHD: ~4 ms empty cave, ~11 ms with 19–22 alive + ~3 ms tick ≈ 60–70 fps, inside budget but the true headroom check belongs to the user's screen.

### M15 — The deep ones (Lamp Man + Angler rework; DESIGN §8.2/§8.5)
- [ ] **Lamp Man:** spawn every 7 shifts mid-tunnel (random normal-width Maze tunnel, never repeats), true-up stance on the floor, lure pixel-identical to the Angler's; seen-then-left despawn; **too-close jumpscare** (sting + randomized rotation/tilt during the whip so the vanish is never witnessed + reserve breath + max HR); 7-shift relocation; unshootable
- [ ] **Angler rework:** slow Maze patrol → freeze-on-sight → vortex inhale (drag into mouth, shake, carry to a different room, max HR, no HP damage) → swims away → despawns out of sight; shot at range = slow deliberate approach + attack; arc-gun bonus (every chain bounce re-targets it) + PaP'd more so
- [ ] New audio: jumpscare sting, vortex-drag suite, lamp-glow presence loop; lamp.1/rem.lamp.1 triggers live (lines generated at M12)
- [ ] Debug: spawn/despawn Lamp Man at edge, force Angler states, jumpscare preview (with a tilt-safe test mode)

### M15.5 — The Undertow (DESIGN §11.1, user idea 2026-07-21 — the cave inhales)
- [ ] **Flow field home:** one-time Dijkstra from the Heart's chamber over the passage graph at Heart-grab (door state respected) → per-node next-hop toward the apse; runtime pull vector = along the local passage polyline down that field (honest water through real tunnels, never at rock)
- [ ] **Surge mechanic:** during the Ascent, every `undertowPeriodSec` (~60–90 s) a `undertowSec` (~8 s) override current ramps in, strength ≫ ambient (knobs in tuning), squeeze-damped per the pin-guard; fighting it spikes HR — the price is air; position-only, never camera; bubbles/gauge stay honest
- [ ] **The theater:** enormous machine-intake audio cue (generate `undertow-surge`; the amb-machinery pump is the same machine's voice — one vast intake), first-time REMORA + Lowe lines (write per LORE §1.3 spread rule, site-works/#1 voids, NO count material), and the cave LIGHTS UP for the duration (string-light flare, biolum surge, global cherenkov lift)
- [ ] LORE addition: the site inhales; the wrongness is the machine sound coming from everywhere including the rock; CANON rows
- [ ] Debug: force-surge button, flow-field arrow overlay, surge state readout
- [ ] Tests: flow field reaches every node (no dead vectors), pull never points into rock along path samples, surge cadence math

### M16 — The Annex (museum, party, concept gallery; DESIGN §12.1)
- [ ] **Museum Annex room** added off gal-rec in layout.json: dry, fully lit, off the pathing graph (true safe zone), and by far the best-dressed space in the game (poured floor, brass rails, glass cases, museum spots)
- [ ] **Exhibits mirror the run:** tapes shelf, tin divers, draught flasks, weapon rack, photograph wall (inspected prints), figures of Drowned/Angler/Lamp Man/Guardian with plaques (a neat hand nobody identifies), roped-off Heart replica
- [ ] **Morale button:** guarded red button → zombie party (dancing exhibit figures, mirror ball, saturated light) + **"Morale Night" generated** (LORE §6.1 lyrics); press again to stop mid-song
- [x] **Concept Gallery — LANDED EARLY 2026-07-21** (billing enabled mid-M11): `scripts/generate-concept.mjs` written, all C1–C12 generated, CONCEPT ART section live in title+pause menus with lightbox + `FILM UNDEVELOPED` fallback frames (`src/game/concept.ts`); all 22 in-world images generated in the same round (g12-sign needed 21:9 — the API rejects 3:1/4:1)
- [ ] Both systems unlocked by default; future gates (first-win / encountered-subjects) documented in DESIGN §12.1, not built
- [ ] Debug: unlock-all-exhibits, party toggle, gallery fill-with-placeholders

### M17 — PARKED: the weapon-feel overhaul (user, 2026-07-21: "we will need a full upgrade to the feel of all the weapons and paps, but that will be later")
Scope when the user calls for it: per-gun recoil/handling character, PaP transformation drama, impact feedback, reload choreography, viewmodel polish. Not before M12–M16 land.

## Post-ship candidates (pick by fun-per-effort)
Endless mode after victory · zombies that cut guide lines · procedural layout variation · Gemini-generated tape-player portrait stills · second silt-trap type · gamepad support.

## Cut list (explicitly out of scope v1)
Multiplayer/co-op · saves mid-run · difficulty modes (the tilt slider is accessibility, not difficulty) · runtime API calls · non-procedural art assets.

## Risks & mitigations
- **Underwater zombie pathing jank** → path on the authored graph, steer only locally inside chambers; debug spawn + chase spot-checks every zombie change.
- **Silt/particle perf** → instanced particles only, per-chamber budgets, measure in M4 not M10.
- **Scope creep in weapons (M6)** → distinct-feel bar, not realism bar; split the milestone before quality drops.
- **ElevenLabs output disappoints** → WebAudio synth fallback listed per-sound in M8; VO is flavor, not load-bearing.
- **Motion sickness from tilt** → honest tells + accessibility cap, default respected in all debug videos.
- **Context loss between sessions** → this file + DESIGN.md + worklog are the only memory; write them like the next session knows nothing.
- **I can't twitch-pilot long real-time runs via screenshots** → pilot mode + headless simulation carry all win/lose and balance testing; manual play is feel-judgment only, with slow-mo/god assists.

## Worklog (append-only; newest last)
- **2026-07-18 — Planning session.** DESIGN.md and PLAN.md written from the user's brief + my redesign (race structure, guide line, honest-tells doctrine). Layout-as-data + map viewer + debug-harness-first chosen specifically to avoid the prior "Venice Beach" failure (assets improvised against an undocumented layout). Keys present in `.env` (ELEVENLABS_API_KEY, GEMINI_API_KEY, DEEPSEEK_API_KEY — names only, never commit). Repo initialized. Nothing coded yet by explicit user instruction.
- **2026-07-18 — Lore session (user request).** LORE.md written before M0: Site BLACKWATER internal truth (1968 flood, the Pile, Project CORMORANT, the Heart), player character Elias "Dutch" Voss with surface-only VO rule + full line list, facility-fused-through-cave setting decision (user asked cave vs. nuclear facility — hybrid keeps organic geometry strengths, facility pieces become landmarks; Throat = the bore), visual language glossary for every game object, 6 final tape scripts, easter egg (3 wind-up toys → rec-room jukebox → random MP3 from `public/music/easteregg/`; Eleven Music attempt + Suno prompt/lyrics for "Still on Shift"), Gemini image manifest (12 images + fallback rule). DESIGN/PLAN updated to reference LORE.md; schema gains poster/toy/jukebox tags.
- **2026-07-18 — Final pass & lock (user request: last chance, then set in stone).** Coherence holes found and closed: (1) the 41-crew vs. hundreds-of-kills mismatch is now the design's centerpiece, not a hole — "the site is always at full complement" (LORE §1.1), burrow spawning is the site keeping its roster, Drowned variants are deliberately few with recurrence as canon (Lowe recognizes repeats), and stats screens became Lowe's ledger (`RECOVERED/ROSTER/DISCREPANCY`); (2) the Ascent is now foreshadowed by a job-sheet clause ("Once the item is lifted… Ascend."); (3) "the site works" established fact retroactively justifies all gamey purchases (greased lockers, potable draughts); (4) G13 added — the 1971 photograph appears on the intro card AND as a physical print pinned at the drill head; (5) VO punched up (tally milestones, Barrow recognition, "like I'm the one that needs recovering"). PITCH.md written (store copy). **All three docs marked LOCKED: build sessions implement/tune/fix, never redesign; forced deviations get loud worklog entries.**
- **2026-07-18 — Lore v2: mystery-first rewrite (user request).** User verdict on v1: too solved, too cliché (dead-father/dog-tags motivation, noble final tape, explained zombies). Full rewrite. Structural change: LORE §1 no longer contains "the truth" — it contains established facts + **eight voids that must never be answered, even internally** (what the Heart is, why the crew moves, the client, the 1971 photograph, the count that comes out 42, the empty condemned suits, disagreeing measurements, the site's purpose), plus the horror-engine rules: mundane voice, exactly one wrongness per artifact, evidence must disagree, the flood is never depicted, and fiction-unreliability never touches gameplay tells (pillar 3 guard). New character: Vernon Lowe, soft-spoken body-recovery diver — motivations are the fee, professional completion of the trade's biggest unfinished job, and being chosen by name (unexplained); he talks to the dead as clients and counts compulsively; fear registers as politeness. Objective delivery is now a clinical job sheet with Lowe's pencil note. Tapes rewritten: six mundane logs, one impossibility each, no flood narrative, T6 is a roster read three weeks after the flood by a voice not in the crew book. Song lyrics rewritten cryptic (no exposition, roster outro). All v1 gameplay-review mechanics kept unchanged (tape playback at safe surfacing, VO anti-spam, intro card slot, manifest hardening, toy shimmer).
- **2026-07-18 — Lore gameplay review (user request).** Reviewed LORE.md as a game system, not fiction. Fixes: tape playback moved out of depth (collect on pickup → auto-play at next safe surfacing; depth playback would train players to skip story); Dutch VO anti-spam spec (120 s cooldown, no-repeat, priority queue — silence is default); added Boone's letter as run-intro card because nothing previously told the player the objective; toys got an ≤8 m music-box shimmer (findability without pixel-hunting); blueprint G10 changed from "subtly wrong" to "honest about its blind spots" (a secretly wrong map violates the honest-tools pillar); G11 prompt de-counted (generators can't count); G9 perk labels generated individually, not as a sheet to slice; posters get bold-headline design + fullscreen inspect overlay with real-text captions so garbled AI lettering can't break comprehension; geiger crackle near the Pile as flavor (explicitly no radiation mechanic).
- **2026-07-18 — Design revision (user feedback).** Door buys restored (DESIGN §10.3: progression + shortcut doors, buy-open/never-close, ~5500 mandatory) — consistent with "passages never close" since doors only ever add passage. Frenzy rule replaced by the user's mechanic, the Cave Stirs countdown (§9): near-empty rounds auto-advance after 45 s, survivors carry over; pillar 2 reworded — I had oversold breath as the anti-crawler mechanic (it taxes chores; only a rule stops round-freezing). M9 rebuilt around headless balance simulation + in-game scripted pilot mode after honestly assessing that I can't twitch-play long real-time runs; manual play is for feel only. Geometry method pinned: SDF + marching cubes for organic/layered passages.
- **2026-07-18 — Movement design addendum (user).** Three locked-in changes for M3: Space/C vertical swim is camera-relative (not world), so tilt disorientation carries into controls; a gentle ambient wandering current pushes the player everywhere below surface (position only, never camera; ~0.35 m/s); swimming must feel momentum-through-water. DESIGN §6.1 updated.
- **2026-07-18 — M3 complete (player: swim, air, light).** Verification-loop catches this session: (1) player floated in mid-air on the shore — swim mode had no gravity above water; added gravity-when-head-above + land-into-walk transition; (2) the "dry shore" had only two meter-wide floor patches — instead of guessing coordinates, PROBED the SDF for actual dry floor and moved the camp room east so its west edge overlaps the pool (water entry) and its east bulk is a real shelf (41 floor patches now); *lesson: when placing anything on geometry, scan the SDF, don't trust node math*; (3) mysterious pale rectangles in screenshots turned out to be the bubbles — untextured Points render as squares, huge at lens range; fixed with a radial-gradient sprite + mouth-offset exhale origin. HUD is DOM, not canvas — canvas screenshots never show it, so HUD verification is DOM queries (pips count, depth text, overlay class). Water discs blow out speculatively at glancing angles — M4's water pass should tone specular. Treading in a pocket bobs around the water line (gravity above / buoyant below) — feels right, keep. Air numbers: full tank ~100 s calm at 1.0× zones — the compact-cave concern from M1 stands; M9 balances.
- **2026-07-20 — M8d complete (user round: 13 asks, all landed; M9/M10 deleted per user).** Root causes worth keeping: (1) **the playtest overlay** was `#menus` itself — the root div is a fullscreen wash and only `render()` ever set the `hidden` class, so any mode that never shows a menu (editor ▶ TEST) sat behind an invisible click-eater; overlays must START hidden. (2) **"Music very quiet in open air"** was the jukebox riding the underwater bus, which ducks to 25% on head-above BY DESIGN — music got its own never-ducking bus where water only changes tone (low-pass 1100 Hz ↔ open). (3) **Zombies ignoring falseUp gravity**: the haul-out branch hardcoded world −Y for the grounded probe, climb clamp, and gravity; all three now run along the region's falseUp (verified: a body in the 90° Listing Room falls along −Z at 2.6 m/s while world-Y stays steering-only). (4) **Editor clicks**: filtering now DISABLES raycast on non-matching nodes/tunnels — ghosting alone still stole every click; audio nodes get 2.4 m invisible pickers and hide by default (♪ toggles). New content: REMORA (LORE §2.4, 22 lines, underwater-only complement to Lowe, helmet-speaker routing so she's never water-filtered), 5 music tracks + 2 scarier beds generated in one background run, pause-menu photograph gallery (posters/G13/toy polaroids), jukebox E-interact that preserves the egg (disabled prompt = the hint). Verification notes: browser-pane `computer screenshot` times out on the hidden WebGL tab — DOM assertions carried the HUD checks (blood opacity 0.481 + `critical` at 22 HP), the standard `bw.shot()` canvas path carried the editor; esbuild rewrites `() => undefined` to `() => void 0`, so never string-match a function body to detect a raycast stub (it "failed" until inspected directly); a 158 s fast-forward on a live shore KILLS an unattended player — force god+pause before long simulated waits (the lull's dead-player refusal was proven by the accident). Fixed in passing: generate-vo.mjs overwrote manifest.json's `sfx` section (now merges). 129/129 tests; tsc clean; console clean.
- **2026-07-20 — M8c complete (images, job sheet, menus) — M8 is DONE end to end.** Gemini blocker diagnosed precisely before building around it: the key is fine, but its image-model quota is literally `limit: 0` (image generation needs billing on the Google account; text-model 404s were a red herring — probe the SPECIFIC capability, and read the quota metric in the 429 body before blaming the key). Response: fallback-first per LORE §7's own rule — `media.ts` draws all 21 images as procedural period prints (paper/stencil/pictogram helpers, ~1 canvas function per artifact) and `imageUrl()` transparently upgrades to generated files once `generate-images.mjs` can run. **Two real bugs found by driving the flow:** (1) the M8c inspect toggle re-opened itself on the closing press — the escape hatch consumed the E-edge but interact saw its own fresh edge on the same physical press; fix = swallow E until release (`ctl.eSwallow`); (2) posters on fixture nodes landed EXACTLY on the fixture (wallSpot is deterministic) — the G5 sheet was inside the Pile breaker until the `exclude` arg put it on another wall, 7.3 m clear. Menus: pointer-lock loss is the pause trigger (free Esc handling; debug-panel users get a pause popup per Esc — acceptable, noted); the animation loop gates tick() so menus freeze the run while `bw.step()` stays live for the harness; tape replay pushes into the normal deck path. Settings wiring: FOV squeeze pull-in became relative (`fov−11`), brightness is a canvas CSS filter (tone-mapping exposure is a no-op under NoToneMapping — cheaper to filter the canvas). Verification: hidden-pane canvas can go 0×0 (shot 400s) — `bw.renderer.setSize(1280,720)` first, then shot. Deferred debts now closed: pause-menu tape replay ✓; still open: buyClick sfx unwired (M9 polish), Lowe menu-idle lines (cut — surface lines cover).
- **2026-07-20 — User feedback round: audio placement, ambience, editor node types, tuning UX (seven items, all landed; session survived a mid-work crash — nothing lost, the interrupted step was re-run from a script FILE after the inline-heredoc shell parse failure that caused it).** (1) **Tape/VO timing REVERSED by the user** (their own earlier rule): tapes now play instantly on pickup at ANY depth (routed to master — a deck held to the mask, no filter, no pause mechanic), and Lowe speaks only after **3 s continuously out of the water** (bobbing ≠ surfaced; close-call lines judge the air you ARRIVED with, since the tank refills faster than the 3 s gate). LORE §5/§2.1 + DESIGN §12 updated with dated notes. (2) **Depth ambience**: 3 generated beds crossfaded on the current's own depth bands (weights verified [1,0,0]→[0.46,0.54,0]@50 m→[0,0,1]@158 m); ducks at the surface via the underwater bus. (3) **Audio-emitter nodes** (`kind:'audio'` + `audio:{sample,radiusM,falloff}`): no carve (SDF skips), outside rules/pathing, positional loop with honest rock occlusion; 4-sample palette generated (machinery/airflow/groan/drips) + 3 placed (the Listing Room airflow the user asked for, Pile machinery, abyss groan — delete/move freely in the editor). (4) **Teaser nodes** (`teaser:true` on nodes/edges): carved but excluded from every check via data.ts playable-filters, ghost-styled and hidden by default in the editor (👻) — marking the 6 teaser-named rooms made the suite FULLY GREEN for the first time (the standing `-copy` floor failure was a teaser all along). (5) **Editor**: node-type FILTER dropdown (boxSpot→exactly 3 labels), +♪ button, dedicated audio-node form with sample datalist, range+falloff wireframe shells (`m8-audio-node-shells.png`). (6) **Tuning panel**: 💾 SAVE TO DISK → `/__tuning` → committed `src/tuning.overrides.json` applied before defaults capture (saved values READ as stock; localStorage stays the scratch layer; watch-ignored like layout.json), and **all 416 knobs now show descriptions** — parsed from tuning.ts's own trailing comments (108 comments added to source), plus a leaf-family dictionary for repeated weapon stats and path entries for single-line literals. (7) **Inspect softlock fixed**: E always closes an open overlay regardless of drift (verified: open → teleport away → prompt gone → E closes). Perf 0.445 ms; 129/129 tests; 28/28 editor checks. Note: the user deleted the placeholder music-box WAV during their listening pass — the folder now holds only the real song.
- **2026-07-20 — Generation round (user: "all audio should be ElevenLabs" + rotated keys).** The keys "failing" again was MY parser: the new `.env` wraps values in quotes (`KEY="…"`) and the loader passed them through literally — strip quotes before crying invalid (lesson: when a fresh credential fails, diagnose the FORMAT before blaming the key; masked-diagnostic printing found it without echoing secrets). `.env` confirmed git-ignored, never in history. Then the full run: 32 Lowe lines + 6 tapes (deliberate casting in-script: Lowe=Bill, the only old American male; T6=River, a NEUTRAL voice for the roster reader who isn't in the crew book), 40 SFX via `scripts/generate-sfx.mjs` (prompt style: ask for underwater/wet in the SOURCE so wetness layers under the M8a bus filter; loops request seamless+loop param), and Eleven Music produced `still-on-shift.mp3` (3.7 MB) — it auto-joins the jukebox folder. Runtime: new `SampleBank` (`src/audio/samples.ts`) + every one of the 28 sfx.ts voices asks it first (rate-jitter variety on shots/moans), synth demoted to per-sound fallback exactly per plan — verified by count instrumentation: sample path taken, then entry deleted → count flat, synth RMS still >0. All 40 decode (loops 8–10 s, shots 0.7–2 s); 78/78 asset URLs 200. Gotchas: sound-generation minimum duration is 0.5 s (geiger at 0.3 s was the one failure); generated masters run QUIETER than my synth (speargun RMS 0.0088 vs 0.021) — expect a gain-balancing pass at M9 with the user's ears. I cannot audit aesthetics: the listening pass is the user's (STATUS line). 129/130 tests; 0.53 ms/frame.
- **2026-07-20 — M8b complete (voices & flavor audio) — AND the session's headline: BOTH API KEYS ARE DEAD.** ElevenLabs and Gemini keys in `.env` are rejected as invalid (tested first, before building anything around them — always probe a key before planning a session on it). Response per the plan's own risk note: the milestone re-split (M8b = systems on mandated fallbacks, M8c = images/job-sheet/menus), everything runtime shipped complete, and `scripts/generate-vo.mjs` committed ready — rotate the key, run it once, and `manifest.json` flips the game onto real audio with zero code changes. **Architecture choice that made this clean: the line/tape text lives in `lines.json`, imported by both the game (subtitles) and the generator (TTS input) — the printed word and the spoken word can never drift.** Systems: VoiceQueue (pure, 11 unit tests pin LORE §2.1: 120 s ambient cooldown, once-per-run, tapeReact>event>ambient priority, 40% roll, never-underwater), TapeDeck (safe-surfacing gate at 20 m, pause-on-dive holds the reel, B-skip still fires the reaction), toys/jukebox (folder auto-listing: dev middleware + build-time snapshot — drop an MP3, zero code; placeholder WAV synthesized in node and committed). Fallback voice = radio squelch + typewriter subtitle paced by the estimated read time. Verification notes: interact prompts must return null (not empty text) when spent; `createMediaElementSource` doesn't exist on OfflineAudioContext — guard every media-element path or the offline-engine harness trick from M8a crashes; a defer test must place the hostile BEFORE the surfacing tick (the deck rolls on the first safe frame — my first "failure" was test order, not code); bench readings after thousands of harness steps are poisoned (1→9→20 ms escalation) — 8 warmup renders on a fresh page then bench(60) gave the honest 0.35–0.5 ms. Known deferred: pause-menu tape replay (M8c), buyClick still unwired (M8c), Lowe VO "menus" trigger surface (M8c).
- **2026-07-20 — M8a complete (audio engine & synth soundscape; M8 split in place per its own size note — M8b holds ElevenLabs/Gemini/menus).** Whole soundscape synthesized: engine (two buses, silt muffle, SDF-occluded positional voices), 29-voice sfx library, one AudioDirector consuming a per-tick snapshot + explicit hooks at main's existing callbacks. **Key verification technique for audio without ears: `AudioEngine` takes any `BaseAudioContext`, so an `OfflineAudioContext` renders the same graph gesture-free and `verify.ts` measures RMS/peak/brightness per voice and per bus — and attaching an offline engine to the live director lets `bw.step()` drive the real game while `audio.recent[]` logs every triggered sound with its computed gain/pan/occlusion.** Gotchas logged: (1) offline contexts have a FROZEN currentTime until rendering, which defeats `setTargetAtTime` ramps (muffle test needed an instant time-constant param) and the 4-Hz occlusion throttle (use a fresh positional handle per probe); (2) `setTimeout`-scheduled tails (reload's second clack, breath exhale) don't land in an offline render — offline stats cover each sound's immediate body only; (3) the round stinger is deliberately silent on the FIRST observed round (page-load guard) — a test that starts rounds from paused sees nothing until the second change; (4) preview tool port mapping lied (51921 vs vite's real 5174) — read preview_logs. Unwired but triggerable: buyClick on wall-gun purchases (shops has no buy callback; M8b's menu pass owns it). The drop chime rides the drops toast wrapper — every drop toast IS a drop event. Perf 0.29 ms/frame with the director live.
- **2026-07-20 — M7 complete (specials, Abyss, win/lose).** The Angler, the Shade, the Guardians, the Heart, the Ascent, both ledger screens, G13 — all verified; the DoD's assisted run was the session's best evidence and its best story (three attempts: unarmed = drowned in the chimney; armed = won after TWO live Second Wind heart-drops, 44 recovered, ledger digit-perfect). **What the run taught, logged for M9:** (1) the ascent through squeeze chokes is brutally tight without a build — the intended pre-Heart loadout (Iron Lungs / Fin Kick / guns / doors) is not optional, which matches §4's arc but needs the difficulty curve confirmed; (2) my route prep missed the mz-gate→mz-stores DOOR and the walker wedged against its plug burning air — door state must be part of any route plan (pilot mode should read `doors[]`); (3) a threat-first fighting policy tangles in busy zones — the winning endgame policy was flee-and-knife (the design's own doctrine, rediscovered by a script). **False-alarm lesson:** the Shade "surviving" clearAll was the game being right — the player was standing on the re-armed chalk column and touch-redetonating the cloud; check the WORLD before blaming the code. Specials absorb shots in front of zombies (no pierce-through) and insta-kill deliberately ignores them; Bang Stick one-shots a Guardian (4000 < 8000) — flagged for M9, they respawn anyway. Screens: `m7-angler-lure`, `m7-guardian-dark`, `m7-biolum-dark`, `m7-shade`.
- **2026-07-20 — M6.5 feel & crowd round (user playtest feedback — five issues, all landed).** (1) **HUD split**: #ui is now two layers — `#hud` (all 20 game elements, always on) and `#dbg` (fps + panel); H toggles ONLY the debug layer (the user found H taking the real HUD with it); screenshots use the new debug button instead. (2) **Crowd**: per-body speed ±15% (pack strings out), positional separation (near-crowd min spacing 0.56 m measured — was literally one point), attacker cap 3 + per-attack windup jitter — grabs measured spread over 11 s, max 3 per half-second window (was 9 simultaneous = instant death). (3) **Stuck recycle**: no-PROGRESS (closer-to-player or path-index advance — raw displacement failed because the unstick nudge itself moves the body, verified) for 12 s and ≥8 m away → burrows back down, ticket returned to `toSpawn`; wedged body recycled at 16 s while a healthy long-router never tripped it. (4) **Viewmodels**: procedural first-person rigs for all 11 guns (Twinfish shows both hands; papped guns carry a lit cherenkov strip), swim bob / fire kick scaled by fire delay / reload dip; **knife swing animation with a REACH ARC drawn at exactly knife range** (honest-tells applied to melee) plus the crosshair flips ·→✕ when a body is in blade reach. (5) **Melee knockback**: knife 4 m/s, lance 5, Bang Stick 7 — breaks attack windups, staggers re-approach; measured 0.68 m shove. Perf lesson sharpened: hidden-tab benches are only honest for the FIRST couple runs after a reload (the compositor queue saturates after ~100 back-to-back renders) — fresh-page cost with 9 zombies + separation + viewmodels is 0.30–0.42 ms/frame. Screens: `m65-vm-twinfish/papped/knife-arc`.
- **2026-07-20 — M6b complete (arsenal & drops).** The Roulette, the Bench, five box guns, six drops — all E2E-verified with numbers (chain falloff 150/105/73/51, vortex spread 1.27→0.02 m, a 6,762 HP body one-hit by the Bang Stick, +120 on a 60-point award). **Design deviation, logged loudly:** DESIGN §10.4's PaP grate needs a powerGate edge and the user's map no longer has one — the BENCH itself is power-gated instead (cold until the Pile is live), preserving the intent without touching their layout; if they re-add a grate in the editor it also works, unchanged. **Verification lesson (three sessions running):** every "failure" this session was again bad scripted CAMERA PLACEMENT, not game code — the fix that stuck was inching toward the target through SDF-checked 0.25 m steps instead of teleport-and-pray offsets; that walker is now the house pattern for interact tests. Card/label planes read edge-on from most angles — billboarding sprites are the default for in-world text now (the box card was rebuilt as a Sprite). PaP ammo rides the existing lockers (papped guns pay 4500); box guns have no locker, so Max Ammo is their only refill — BO1-faithful, M9 judges. Screens: `m6b-box-offer`, `m6b-box-toy`, `m6b-pap-glow`.
- **2026-07-20 — M6a complete (economy core; M6 split in two per its own risk note — M6b holds box/PaP/drops/feel-polish).** The money loop is real: context-sensitive E interact (prompt suppresses E-roll — the M4.7 debt), door buy-grinds (crumble + fade + silt puff; zombie pathing unblocks live, 26 s → 11 s through the bought artery), SDF-wall-mounted lockers/vendors with canvas chalk-slates, brass-canister perk stations (caps emissive with power — the only saturated things down there), the Pile's breaker → 39 artery bulbs, slot-based arsenal with five mechanically distinct wall guns, all 9 perk effects wired through one PerkMods object every system reads, Second Wind end-to-end. **Bug found by measurement, not eyeball:** a new gun inherited the OLD gun's pending fire delay (Pneu measured 3 rds/s instead of 10; harpoon/lance "missed" because they never actually fired inside the test window) — the raise beat now REPLACES fireT on give/switch; regression test pins it. **Map flag for the user:** their authoring round rewired pap-chamber onto throat-rim-air and dropped the powerGate edge entirely — power→grate is implemented generically and binds to any powerGate edge, but the current map has none, so PaP currently sits behind no gate (M6b needs this resolved before the Bench ships; related to their open teaser-room item). **Console-ghost lesson:** the two NaN bounding-sphere errors "reappearing" every check were STALE tool-buffer entries from M5's invalid test placements — they never multiplied across reloads and instrumentation caught zero live NaN; before chasing a console error, check whether it GROWS. Affordability DoD by pure math in tests, not manual play: power path ×2 covered by round 3, 2–3 perks by rounds 8–10 (the §4 arc). 114 tests (113 pass + the standing user-map teaser item). Screens: `m6-locker-speargun`, `m6-door-grind`, `m6-string-lights`, `m6-perk-station`, `m6-power-board`.
- **2026-07-19 — M5 complete (zombies & rounds).** The Drowned are real: procedural box-rigs (3 variants, deliberately few), A* on the graph + polyline steering, burrow spawning, grabs, corpse drift, workstation pauses; rounds with intermissions and the Cave Stirs, all E2E-verified. **Pathing lessons (both caught by scripted chases):** (1) steering at the next VERTEX pins bodies on curved-passage walls — follow the polyline instead: project onto it, aim a lookahead point ALONG it (the M4 blind-line-follow lesson, re-learned for AI); (2) squeezes additionally need a short lookahead (1.1 m) + wall-slide steering (strip the into-wall component of intent) or the crawl grinds to 0.1 m/s — with both, the worst chase (maze burrow → hub through a winding squeeze) went 31 s mostly-stuck → 13 s clean. (3) Surface decay needed a grounded-crawl rule: land gravity pinned climbers at the waterline 2.7 m short of the shore player — rock-underfoot probe → no gravity + slope-following intent, shore reached in 10 s. **Verification traps:** three consecutive "bugs" were bad test placements (zombie posed inside rock, camera set into rock, teleport-touching the very column I meant to shoot) — probe the SDF before scripting ANY placement; mid-tick raycasts need `rig.updateWorldMatrix()` first or shots test last frame's bodies (verified miss → hit); and tens of thousands of hidden-tab harness renders accumulate GPU-queue backlog that poisons `bench()` (a scary 23 ms/frame vanished on reload — always bench on a fresh page; 9 zombies ≈ 0.09 ms). Melee landed on RMB/V and DESIGN §6.6's wording was aligned (M4.7 locked "F is lamp-only" after user verification; the knife is a weapon → mouse). Kept for M6: weapons module already handles defs/ammo/reload generically. Known: the pre-existing `throat-rim-air-teaser-copy` floor-test failure stands (user's in-editor teaser room, their call). Screens: `m5-drowned-swim/reach`, `m5-pool-swarm`, `m5-cave-stirs`, `m5-corpse-drift`.
- **2026-07-19 — M4.9 editor round 2 + tilted water (user playtest of the editor).** (1) **Tilted rooms now tilt their water** (design change, user-directed: falseUp no longer "lies" against an honest surface — the water itself follows the lie). `buildAirWaterMap` returns full surfaces `{y, up, center}`; `waterLevelAt` evaluates the tilted plane at (x,z), so float/breach physics agree with the visual; pool discs are the room's own **ellipse cross-section** (not max-radius circles that spilled out of stretched rooms) oriented to falseUp — game and editor draw identically. Verified numerically in the Listing Room: level −53.74 west / −54.89 center / −56.05 east. (2) **THE RANDOMLY-INVISIBLE TUNNEL LINES, root-caused:** the invisible fat click-pickers (opacity 0) still WROTE DEPTH hugging each line exactly, so transparent sort order decided whether a picker occluded its own line — hence "random". Pickers now depthWrite/colorWrite off; room spheres depthWrite off; lines frustumCulled off + renderOrder 1. Lesson: *an opacity-0 mesh is not invisible to the depth buffer.* (3) **Double-click waypoints** rebuilt as a screen-space nearest-segment search (14 px budget) — raycasting 0.6 m cylinders was why tunnels "couldn't" get waypoints. (4) **Squeeze bends are unplayable** (no room to turn around): new rule check flags >35° bends mid-squeeze; it immediately caught 4 real spots → 6 turnaround rooms (`*-elbow-*`, r 2) split those edges in layout.json; editor waypoint panel gained "⭘ → room" to do the same interactively. (5) **Orient gizmo (R / ⟲)**: TransformControls rotate rings drag a selection's falseUp (water follows); 🧭 toggles water/falseUp markers (hidden by default). (6) **▶ TEST playtest loop**: unsaved layout rides sessionStorage into `?playtest=1` — noclip+god at the exact editor camera, F4 returns to the editor with camera AND unsaved edits intact (round-trip verified). (7) **Every TUNING number is editable** in editor + debug panels (`tuningPanel.ts`); overrides live in localStorage (mutate TUNING in place, survive the playtest hop, amber-highlighted, reset button). (8) Hub dropdowns explained in-panel + stale hub ids surfaced with ⚠ instead of silently showing the first room. Vitest note: `data.ts` now touches `location`/`sessionStorage` — typeof-guards required for node.
- **2026-07-19 — Line network: strands, forks, ties that matter (user round 13: "tieing off doesn't really do anything… only supports one line and cannot fork… reeling sometimes skips when the line is jumbled").** GuideLine rebuilt around STRANDS (independent polylines). (1) **Forks**: T tap near a strand's middle starts a new strand tied on at that point (junction knot); follow latches strand+direction on engage so junctions never silently switch you — release and re-grab to take the branch. (2) **Ties now have a real job: they PIN the line** — reeling collects only back to the nearest tie and parks there (`⟲ PINNED · tap X cuts the tie`); X tap when not laying cuts the nearest tie. Anchor knots (index 0) aren't cuttable — you recover them by reeling to them. A strand that never left the hand is discarded on stop. (3) **The jumble bug root-caused**: reelVelocity used nearest-segment over the whole polyline, so a doubled-back line within grab radius (1.5 m) let the glide jump tracks and ride without collecting; reeling now walks the strand STRICTLY from its end (target = next point down; regain the end if the wind-in outruns the hand) — regression test lays a 15 m out-and-back hairpin 1 m apart and recovers all of it. Renderer moved to LineSegments (multi-strand). E2E in-game: lay 13.7 m with a mid tie, fork a branch off the middle (toast confirms), reel the main end → parks at the tie at exactly the protected length, cut, stow; branch survives as its own strand; meters conserved (199≈200 minus the branch). Debug-logging lesson: the "failing" jumble test was actually a wrong assertion (corner quantization: 30.4 m ≠ ~31 m expected) — print before patching. Known v1 quirk (in DESIGN): reeling a parent out from under a fork leaves the branch anchored mid-water.
- **2026-07-19 — Line controls rework (user round 12: "really easy and intuitive… while in a panic… no overlap with shooter mechanics").** The whole line system is now TWO keys, both clear of the combat set: **T tap** = lay/stop/resume by context with INSTANT auto-anchor to rock within 2 m (the 4 s Ctrl+F ceremony is deleted — panic-hostile); **T hold** = ride the line (unchanged latched-direction follow); **X tap** = instant tie-off mid-lay; **X hold** = reel — no longer "toggle then swim backwards": an active glide toward the anchor that collects line as you go, releases to a stopped line, homes the last stretch onto the anchor to stow. Freed for the shooter: R (reload — now only restart-when-dead), F (flashlight only, melee later), C (swim-down only); Ctrl grab is a pure movement brace. The HUD kit line teaches the current options in place (`LINE END · T resume · hold X reel`). Two real mechanics bugs found by the new tests: (1) reeling's wind-in radius (2.2 m) outruns the grab radius (1.5 m), so the retreating end slips out of the hand — reelVelocity now chases it; (2) with only the anchor left there's no segment to follow — it homes the final stretch. E2E-verified in-game: anchor toast at a wall, 12.6 m laid, tie, stop, 3.8 m ridden, reel 13→6 m, release, full recovery with the reel back at exactly 200 m; F only toggles the lamp. KeyX added to the fullscreen Keyboard Lock. Tap/hold threshold and anchor reach are tuning knobs.
- **2026-07-19 — falseUp gravity & jump (user round 11: the 90° fix "revealed" the body still obeyed true down).** The whole land-body frame now runs along the smoothed reference up: walk mode flattens wish onto the fake ground plane, jumps take off along the fake up, gravity pulls along the fake down, feet/eye offsets and ground-snap follow the same axis; swim-mode breach gravity/buoyancy/surface-damp act along refUp too, and the surface-into-walk probe looks "underfoot" in the lie's frame. With refUp = world up every expression reduces exactly to the old code, and shore walking/jumping verified unchanged. In the user's 90°-listed mz-air2: a dropped player's fall curves onto the fake floor as refUp eases (~0.5 s smoothing doubles as a nice transition), lands in walk mode standing on rock, and a jump measures takeoff (0,0,6.3) — 1.43 m rise along +z, 0.00 along true up. Slides deliberately stay honest-gravity. Also this session: committed the user's map round (decoys + teasers, §5 all green again); known open item: `throat-rim-air-teaser-copy` floor overlaps `throat-rim-air` and trips the floor-flatness geometry test — possibly an intentional teaser window, user to confirm.
- **2026-07-19 — 90° water fix + editor now in the milestone list (user round 10).** The user rotated mz-air2's water fully vertical (falseUp [0,0,1]) and drowned in the air half: `waterSurfaceLevel` had a "steep tilt → treat as flat" fallback that collapsed a vertical surface into a horizontal one. Fix: the plane branch is exact for any up with up[1] ≥ 0.05 (y-level = y − s/up[1], s = signed distance), and steeper/inverted planes classify by signed distance directly (AIR_LEVEL_Y / SUBMERGED_LEVEL_Y — a vertical surface splits a room SIDEWAYS, there is no y-level). Verified in-game against the user's edited map: air side breathable 3 m below center, water side submerged above center, tunnel region inherits the vertical plane. New `cave/water.test.ts` pins flat/30°/90°/inverted/air/gap behavior. Also: the level editor was never in the milestone plan — added as "M4.8–M4.9 ✅ (unplanned)" per user request. NOTE: the user is actively building in the editor (uncommitted layout.json with false-chimney decoy rooms); 4 §5 checks fail on their in-progress map — that's their live authoring, deliberately left uncommitted.
- **2026-07-19 — M4.9b water model v2 (user round 9: "waterY doesn't make sense anymore").** Water is now RELATIVE, like `floor`: rooms carry `water` (fill fraction 0..1 along their false-up — moves and tilts with the room; slightly negative = surface down the entrance shaft, preserving the bell behavior); `dry` alone = fully air, no water field needed. Tunnels swap `waterY` for `airGap` (meters of air hugging the CEILING, following the passage profile) — the absolute plane only made air where a tunnel happened to cross it, which is why the maze gaps "didn't work"; verified in-game: level tracks ceiling−gap along the whole passage. Slides keep `waterY` as their absolute plunge line. `WaterSurface` is now a union (plane | air | gap) with `AIR_LEVEL_Y` for bottomless air regions; `data.ts` auto-migrates legacy fields at load and layout.json was migrated on disk (4 rooms became cleanly "fully air"). All water visuals moved to shared `cave/waterViz.ts` (room ellipse discs, gap ribbons, plunge discs) — game and editor render identical meshes. **The water GIZMO the user actually asked for**: W (💧) attaches a single arrow along the room's up at the surface; dragging it writes the fraction and re-renders the surface live (drag math verified: 2 m along-axis = Δ0.165 at ry 6.05); on a non-water room it first makes it half-full dry. Lesson: when the user says "gizmo," they mean a manipulator, not a visibility toggle.
- **2026-07-19 — Ctrl+W hotfix (user report: closing the tab mid-dive).** Ctrl (wall-grab) + W (swim) is the browser's close-tab shortcut — a binding landmine from M4.7. Pages cannot preventDefault it; the real fix is the **Keyboard Lock API, which only works in fullscreen**: clicking to play now also enters fullscreen and locks the game keys (W/A/S/D/Q/E/R/T/F/C/G/N/P/H/Space), so Chromium delivers Ctrl+W / Ctrl+R / Ctrl+T to the game (`navigator.keyboard.lock` verified available). Off by preference via the new `fullscreenOnPlay` setting (debug toggle, persisted). Fallback everywhere: `beforeunload` confirm once a dive has started (skipped when dead so R-restart stays instant). The editor prompts only with unsaved changes (dirty flag, cleared on save). Verification note: fullscreen requires a REAL user gesture — the synthetic-click path was verified error-clean and the API availability confirmed; the actual capture engages on the user's first click.
- **2026-07-19 — M4.8 THE LEVEL EDITOR (user request: "make a tool for me to easily design levels").** Two-part build. **(1) Layout migration:** the authored-coords-×1.7-scale pipeline retired — the live post-scale NODES/EDGES were dumped from the running game through a new `/__layout` dev middleware into `src/cave/layout.json` (world units, 78 nodes / 92 edges / zone hubs), and `data.ts` became a typed loader + helpers (`refreshNodeMap()` added for live structural edits). All 68 tests pass unchanged against the JSON — migration provably lossless. Editor coordinates = game coordinates forever. **(2) The editor (`?edit=1`, `src/editor/{editor,panel}.ts`):** orbit camera, click-select rooms/tunnels/waypoints, TransformControls gizmo drags them (r170 note: `getHelper()`), shift-click connects tunnels, double-click inserts waypoints, +ROOM/DEL/duplicate/rename (rename cascades edges + hubs; hub deletion blocked with a pointer to the hub picker), Ctrl+Z undo (JSON snapshots), per-field property panels for EVERYTHING (zone/radius/stretch/pillars/spikes/floor/dry/waterY/falseUp/tags/contents; width/tilt/door/powerGate/slide/airgap-waterY), live DESIGN §5 rule checks in the panel, node labels, water-line discs, falseUp arrows, ⛰ ROCK button generates the real SDF mesh in-editor (~4 s), 💾 SAVE POSTs the layout back through `/__layout`, ⬇ JSON exports. **Save-vs-HMR lesson: the first save hot-reloaded the editor out from under itself** (layout.json is in the game's module graph) — `server.watch.ignored` now excludes layout.json; game tabs reload manually (toast says so). E2E verified: add room → connect → save (file grew to 79) → undo ×3 → save (file back to 78, zero strays) → tests green against the edited-and-restored file; rock preview and selection gizmo screenshots in `docs/screens/m48-*`. MAPPING.md rewritten around the editor; DESIGN §16 map-as-data updated. Note for future maps: multi-layout selection is a small URL-param change, offered to the user.
- **2026-07-19 — M4.7 manual roll, deception spaces, line/grab UX (user feedback round 7 — "It feels amazing! I love it").** (1) **Q/E = manual camera roll** (70°/s, tunable); **X auto-level REMOVED** — leveling is now player skill (surfacing still auto-levels; that was liked). M6 note: E doubles as roll — door-buy interact must be context-sensitive or use another key. (2) **Traditional mouse when breathing**: head-above-water and noclip re-project look to yaw/pitch in the reference-up frame (pitch clamped, roll preserved-but-decaying) — no free-look tumbling in air; **N (noclip) now auto-enables god** and levels to true up. **Max-tilt-0 spin bug fixed**: roll is undefined looking straight along up (gimbal pole); the tilt step is gated there. (3) **Deception pipeline (`falseUp` on nodes/edges)**: regions can lie about up — the SDF floor plane, spikes, and the camera's reference-up all follow the lie while water surfaces and bubbles stay honest (pillar-3 note in DESIGN §6.5). Built: **the Listing Room** (mz-air2 enlarged r4→5, tilted 30°, two unreachable side-mouths, LORE row added) and **two maze tunnel air gaps** (mz-a~mz-b, mz-e~mz-f: edge waterY leaves a breathing sliver at the ceiling; skewed refUp rolls you off-true when you surface — verified 9° deceived). (4) **Line/grab UX redesign** (DESIGN §6.6 rewritten): R lays/stops line ANYWHERE (no anchor gating — tieOff node tags are decor now), Ctrl wall-grab freezes against the current (verified 0.000 m drift over 4 s), grab+hold-F 4 s = anchor/tie ceremony with TYING % progress, grab-end+F resumes, grab-end+C reels in, T-follow LATCHES direction at engage (look around freely — verified traveling with the camera pointed away). HUD kit line always states the mode; big center toasts confirm every action (probes included — "no indication it worked" fixed). Follow-latch lesson: key edges BETWEEN ticks are lost — engage-while-held beats rising-edge detection in a harness-driven game. (5) **Map fixes**: pap-chamber was INSIDE the cathedral's ellipsoid (its gate floated mid-water — user report); moved west with 5.9 m of rock and `gateAt: 0.75`, gate verified sitting in the passage region. Turnaround bulbs r 1.5→2.2. (6) **Silt-outs thicken the water**: mote buffer 9000 with base density ~2/3, silt fills the rest (verified 5940→9000, +52%). (7) **docs/MAPPING.md**: the user asked to design levels themselves — they already can: data.ts + live reload + N survey mode + ?view=map rule checks; guide written; multi-map loader offered as a small future change.
- **2026-07-19 — M4.6 free-look, squeeze cone, ghost-wall hunt (user feedback round 6).** (1) **Free-look rework:** orientation is now a single quaternion; the mouse rotates about the CAMERA's own axes, so under 180° tilt mouse-up still moves the view up on screen (verified numerically: at roll 90°, look delta lands 0.296 along camera-up, 0.000 along world-up). Walking re-projects to yaw/pitch (clamped ±86°) each frame, which also kills free-look roll accrual on land. The tilt system became a pure step function on MEASURED roll (camera roll however acquired now drifts/decays/re-levels — mouse-accrued roll included); the controller applies deltas about the view axis. Scripts note: `bw.tilt.rollDeg = x` no longer steers — use `bw.player.setRollDeg(x)`. (2) **Squeeze view cone** (the §5 no-turn-around rule, deferred since M2, now real): passage tangent from edge polylines with sign locked on entry and continuity through waypoint bends; mouse gating `exp(−4·(θ/30°)²)` outward, free toward center, hard clamp at the rim, 100°/s forced-forward pull when entering off-axis. Verified: 120 spam deltas pin at exactly 30.0°, one centering motion recovers 6.3°. (3) **Current depth bands** (user knobs): ×0.7 / ×1.0 / ×1.7 at 0–50/50–100/100+ m, smoothstep-blended over 15 m, all in `tuning.currentDepth`; unit-tested. (4) **Motes**: 6000 in a 10 m box, custom ShaderMaterial with camera-distance fade (bright ≤1.6 m → gone by 7.5 m) — dense water without walling off the view. (5) **Noclip = survey mode**: fog off, white ambient 1.5, motes/rays hidden — the whole cave reads like an X-ray (user request). (6) **Ghost-wall hunt:** probes now PERSIST (POST `/__probe` → `docs/probes.jsonl` + localStorage — the user's probes had died with a reload; theirs from this round were lost, future ones reach the repo). Systematic 760-ray field-vs-mesh scan found the mechanism: **noise-thin rock between adjacent cavities drops out of the mesh (sub-cell sliver = no sign change) but still collides** — see-through-but-solid. Worst: mz-d5's ceiling grazed the Chapel bowl (see 19.6 m, collide at 2.3 m!). Fixes: mz-d5 moved properly below the Chapel (now a deep under-Chapel cache pocket), mesh cells 0.6→0.5 m (gen 2.4→3.5 s, 281k tris, render 0.14 ms). Ghost rays 8→4, mean mismatch 0.156→0.128 m; the rest are burrow-crack shells and one far-field abyss ray — waiting on real player probes. Debris door meshes densified (19 packed rocks): see-through gaps over a solid collision disc at every closed debris door were themselves invisible walls.
- **2026-07-18 — M4.5 world coherence & dry caves (user feedback round 5 — MAJOR map rework, user-directed deviations from locked docs, all logged).** The user found the air pockets incoherent ("water above and beside the air with nothing in between") — confirmed root causes: dry rooms merged with the rooms below them (overlapping ellipsoids), fat 'open' entrances, and ellipsoid-membership water levels. **New air/water model:** every air region declares `waterY` in data; `waterLevelAt` is REGION-based (SDF regionAt ref → level map, `buildAirWaterMap`), entrances narrowed and arriving from below, water discs drawn only where a line cuts a cavity. Air bells got FLAT FLOORS (`floor` field, smooth-max SDF carve — also kills the sphere-room look the user flagged) with the pool as a hole where the shaft pierces the floor; benches deleted (floors supersede). **Load-bearing geometry rule discovered: a walkable room's floor must sit ~one tunnel-radius below the arriving passage centerline or every mouth becomes a trough/unclimbable ledge** (jump max ≈1.75 m — a 2 m ledge softlocks). The small breathing pockets (gal-air2/3, mz-air1/3, abyss-air) were NEVER functional before (no water level → could not surface); they work now (waterY = dome mid). **New content per user request:** the Dry Reach — walkable open-air section off the west bell (dry-hall → tall dry-gallery) with stalactites/stalagmites (tapered solid cone prims, path-clearance-checked), two unreachable teaser mouths 6–9 m up the gallery walls, and the WET SLIDE (`slide` edge): zero-traction walk region, ~9 m/s² downhill, verified one-way (scripted escape attempt failed at the waterline) with the return route via a new plunge-pool tunnel to gal-ring-w; two long turnaround-bulb dead-end squeezes (gal-spur-empty2→gal-turn, mz-d2→mz-turn). **Feel changes:** tilt auto-re-levels at 45°/s whenever head breaks water; chalk mounds → chalk COLUMNS (floor-to-ceiling, orientation-neutral — user: "pillars don't tell you which way is up"; same trap mechanic, touch = point-to-axis distance); current now lives in shared `player/current.ts`, scales ×0.45→×1.5 with depth (measured 0.46→1.02 m/s avg), and ALL particles ride it (motes at full current speed + jitter, silt at ×0.8); motes ×2 count, density scales 0.3→1.0 with depth, cyan in the deep. **Underground air ≠ daylight:** new CAVE_AIR env (near-black, ambient 0.3) for head-above-water outside the cenote cylinder — the first slide screenshot had a sky-blue background inside a lightless chute. Doc deviations (user-authorized): DESIGN §5 air/slide/turnaround rules, §6.1 current+slide, §6.2 bells, §6.5 auto-relevel, §7.2 columns; LORE §4 rows (columns, bells, the Dry Reach). Verification: bell surface→climb-out with roll 120°→0 auto-level, 4 walked legs to the dry gallery, slide ridden end-to-end + escape-failed + swim-back via the plunge pool, squeeze→bulb swum (clearance 1.39 m in the bulb vs 0.29 in the squeeze), 66 tests green, ~0.09 ms/frame. Screenshots: `docs/screens/m45-*.png`.
- **2026-07-18 — M4 complete (atmosphere & impairment).** All five systems in one session: zone fog grades, silt (ambient + silt-out + mounds), tilt, guide line, chemlights — plus the M2.5 lighting debt. Design catches from writing tests BEFORE wiring: (1) while laying line you are ALWAYS at the free end, so the reel-toggle would have swallowed every tie-off — pin-at-spot now wins over reel-toggle; (2) follow mode would have PAID OUT new line while gliding along the old one (double consumption, reel bled dry by traversal) — pay-out is gated off while following, re-reel still works. Implementation readings, logged per the lock: intermediate tie-offs require tieOff-tagged spots (schema §16 has the tag; "tie off at intermediate points" read as those spots, not anywhere-on-rock); TUNING beam angle stored as full-cone 84° to preserve the M3 look (DESIGN's 60 was a pre-visual guess; the narrowing intent 84→25 is intact). Lighting: the M2.5 "blows out point-blank" debt fixed with a cone-probe exposure (5 SDF rays across the beam, exposure = clamp(wallT²/20) — quadratic cancels the 1/d² blowout, constant apparent brightness inside ~4.5 m; a single center ray was NOT enough, off-axis close rock still flared). Silt visuals: FOG does the visibility collapse, one camera-following 2.6k particle cloud sells the texture — no per-chamber particle bookkeeping, budget untouched. Verification technique learnings: teleporting the camera back AFTER an exhale strands the bubbles out of frame (pin position DURING the burst instead); a still can't prove bubble direction — shoot a 3-frame sequence; scripted maze navigation = aim-at-waypoint + held W + step(5) loops, and blind line-following = aim at the line point ~4 indices behind nearest, hold T (134/134 frames stayed on-line at vis 4). Harness gained `key(code,down)` for held keys. launch.json now uses autoPort (another chat held 5173; vite ignores the PORT env var — read the real port from preview_logs). Chemlight pooled lights read STRONG (whole small room goes green) — kept for horror readability ("your greens are unambiguous"), M9 judges. Vis-lerp rate 1.2/s means the fog needs ~2 s to catch a zone/silt change — reads as the water thickening, keep.
- **2026-07-18 — Collision-mismatch investigation (user asked when to fix "invisible walls").** Measured properly: 232 rays across 10 rooms comparing field-march distance vs mesh raycast. Result: mesh and collision agree within ±13 cm for 80% of samples (median −5.6 cm) — NO systemic offset worth a rework; the one +8.9 m outlier was a closed door's plug (its visual is a separate door mesh, so covered in-game). Conclusion: the residual feel issues are specific spots, not a bias. Shipped the tool to find them: **P key = ghost-wall probe** — logs position, view direction, collision-wall distance vs visual-wall distance, and region to console + `window.__bwProbes`. When the user reports a spot, read their probes and fix the exact geometry. (Also: the earlier chord-bias theory was wrong — measure before theorizing.)
- **2026-07-18 — M3.8 feel polish (user round 4).** Current: shift rate ×4 slower (timeFreq 0.03), strength floor REMOVED (real lulls; peak unchanged). Heaviness halved (thrust 3.2/5.6 — 1.2 s to cruise). Lunge redesigned as a bounded projection: from rest → lungeMaxBoost (3.0, measured 3.17); against full speed → cancels to ~0 (measured 6.50 → 0.62; the first cut overshot through zero to +3, caught in verification). Surface physics: buoyancy spring + near-line damping float the head at a steady +0.18 m (treading works, full thrust while treading — cut only when properly airborne); splash brake on entry (dive re-entry now bottoms at −2.2 m vs the −14.6 floor). `tuning.ts` player section reorganized with plain-English groups so the user can tweak feel themselves (it hot-reloads live).
- **2026-07-18 — M3.7 heavy physics (user feedback round 3: "300 lb object", "current as constant challenge").** Lerp steering replaced with true force physics: thrust 1.6/2.8 m/s², drag 0.12/s. Measured in the bore: 6 s from rest to top speed, coast retains 70% over 3 s, reversal takes seconds, wall impacts bleed the into-wall velocity component. Two bugs found by measurement: (1) the speed cap was braking coasting bodies as streamline decayed — cap now applies only while thrusting (or in squeezes); (2) a "stuck at y=-80" mystery turned out to be the strengthened current shoving the diver into the bore wall — correct behavior, but it exposed that peak current (floor 0.9, peak 2.4 m/s) could pin a player in a squeeze forever → current damped ×0.3 in squeezes. **Testing lessons: the test diver DIED mid-suite from cumulative air drain across scripted runs (set infiniteAir+god for physics tests), pillar halls and small junctions are not "open water" for physics measurements (use the bore), and after HMR edits do a hard reload before trusting measurements — one run produced garbage from stale module state.**
- **2026-07-18 — M3.6 movement rework (user feedback round 2).** (1) **Streamline momentum**: holding a direction builds speed to sprint-speed even unsprinted (verified: 6.48 m/s and full momentum after 4 s of plain W); direction change dumps it (120° turn → 3.63 m/s, momentum 0.23); sprint builds it ~3× faster — sprint's identity is now buildup+hold+lunge, not a flat speed. (2) **Walk jitter fixed** (the real bug: gravity re-applied every grounded frame + binary ground detection → oscillation; now gravity only airborne + ground-snap): standing drift 1.6 cm over 3 s, five consecutive jumps at exactly 1.69 m with coyote time; jump raised for dolphin dives. (3) **Breach**: first pass launched the player 8 m into the AIR (you could keep swimming skyward) — airborne thrust cut to 10% + full gravity → clean 1.44 m dolphin arc, run-jump dive off the shore verified. (4) Current ×3 (peak 1.2 m/s). (5) Squeeze lunge factor 0.4→0.7. (6) **Invisible walls (user report)**: named cause — door plugs were 0.6 m wider than their visible meshes (invisible blocking ring at every closed door); plug pad now 0.5 with visuals matched to 0.15 of the plug, player radius 0.45→0.42, mesh cells 0.7→0.6 so the render tracks the collision field closer. If ghost walls persist anywhere specific, ask the user for a location and probe the SDF there.
- **2026-07-18 — M3.5 player-feel revision (user feedback).** New systems, all user-requested: (1) **heart rate is now the oxygen clock** — rests at 60, sustained sprint builds toward 150 over ~8 s, lunges +20 and hits +30 via a decaying spike, all approached with ~2 s lag (rise) / ~7 s (recovery); air drain = base × (HR/60) × zone, replacing the flat sprint multiplier; HUD shows a BPM readout with a glyph that pulses at the actual rate; bubbles exhale faster at high HR. (2) **Reserve breath**: at 0 air the bar refills once, flashing red ("!!"), burns in ~8 s with HR pinned near panic (the hook for Lowe's M8 panic audio), then drowning; re-arms only after refilling past 50. (3) Movement silkier (accel 0.55 / glide 1.5 — measured exactly on-curve), **lunge** on sprint-trigger (+3.5 m/s, 1.2 s cooldown, ×0.4 in squeezes, verified), **jump** on land. (4) Current now shifts quickly in direction AND strength (verified varying 0.12–0.20 m/s across 2 s windows). (5) Dry pockets are FULL rooms (r 4, stretched) with solid **benches** — placement picks the clearest of four sides; first version's z-radius outgrew its clearance margin and the dense polyline test caught it within seconds (benches now direction-safe). Dry floor: 17/16/29 patches per pocket. All spec numbers verified in-browser; 45 tests pass. Milestone-tracking note: user asked whether milestones are checked — they are, in this file (M0–M3 ✅); pointed them to the STATUS line.
- **2026-07-18 — M2.5 geometry revision (user playtest feedback).** User played the M2 build and reported: door plugs blocking neighboring free squeezes, everything sphere-shaped ("divots", the Abyss not abyssal), map too small, and a wish for dry land. All addressed: (1) **door plugs are flat discs** aligned to the tunnel axis (spheres bulged past the door plane — that was the reported bug), with an optional `at` arc-position per door; (2) **world scale ×1.7** with progressive radius scaling; (3) **rooms are ellipsoids** (`stretch`), big rooms get large-scale wall warp, and rooms can hold **pillars** — generated with *guaranteed ≥1.1 m clearance from every authored path* (first pillar pass stood columns inside the PaP chamber and a tunnel mouth; the new dense polyline tests caught both); (4) **dry land**: the sinkhole has a real walkable shore at the waterline, and three air pockets are dry rooms with local water lines (`dry`); burrows shrunk to squeeze-width cracks. New hard regression: with ALL doors closed, every non-door edge must be fully swimmable end to end — this test alone surfaced three real chokes (maze shortcut rerouted as an eastern bypass arc; abyss burrow had been authored on top of the power gate). Cathedral is now ~40×57×35 m and finally reads as an abyss. 127k tris, ~810 ms gen, 0.07 ms/frame. Headlamp blows out at point-blank range in small pockets — M4 lighting must add distance attenuation/exposure handling.
- **2026-07-18 — M2 complete (geometry & collision).** Surface nets over the analytic SDF instead of marching cubes — no 256-case tables to get wrong, same watertight result; ~60k tris in ~210 ms so regeneration is effectively free. **Collision collides against the SDF itself** (gradient push-out), not the mesh — one source of truth, no BVH needed; squeeze detection reuses the same region query. Findings the loop caught: (1) the sinkhole was generated as a sealed dome — LORE demands an open cenote, so `SKY_SHAFT` is now part of the map data and the from-below beacon shot works; (2) a door plug would have choked the abyss free-squeeze bypass — waypoint moved (and a regression test now guards every free alternate against all-doors-closed); (3) first daylight pass was blown out — lights need screenshot iteration every time, budget for it. Deferred to M3 deliberately: squeeze no-turn-around + camera pull-in (needs a real body, not freefly). The abyss/Cathedral reads as near-black void with a weak headlamp — correct for the design, but M4 must add the biolum field or verification screenshots there will stay useless.
- **2026-07-18 — M1 complete (cave data & map viewer).** Layout authored first-try against the assertions (only the chalk-mound count needed a pre-emptive fix) — the author-against-assertions workflow works; keep it. **Additions the rules forced:** the two-route rule demanded a second way into the Abyss, so the cave gained "the Chimney" — a free, nasty, squeeze-and-tilt natural fissure from the White Chapel down to the Cathedral (also a great ghost-build route; purely data, geometry comes in M2). Schema grew `powerGate` (PaP grate opens with power, not points), `guardianPost` and `surface` tags, and vendor/cache/toy/tape/poster content fields — implementation-level, logged per the lock. Perk stations: 9 single-perk stations distributed G2/M4/T1-rim/A2 (DESIGN zone table said 2/3/–/1 before there were 9 perks; superseded by data, which is the layout source of truth). **Scale observation for M2/M9:** the authored cave is geometrically compact (~100 m of route depth); air-rule distances pass trivially, so the intended "45 s from air" pressure must come from M2 winding waypoints and M9 drain tuning, not raw node spacing. Viewer learnings: blueprint canvases post to /__shot like the WebGL canvas; `__bwMap` is the viewer harness. Vite 6 + TS strict + three r170 + Vitest; placeholder chamber; freefly; debug panel; tuning.ts. **Critical harness learning: the Browser pane can be hidden (`document.hidden`), which throttles rAF to ZERO — screenshots time out and the game loop never ticks.** Solution now standard: `window.__bw` harness (renderOnce / shot / bench / step / look) + a vite middleware `/__shot` that saves canvas PNGs to `docs/screens/` — ALL future visual verification goes through `__bw.shot()` + reading the PNG, and `__bw.step(frames)` drives real update ticks with synthetic keydowns. `__bw` is the seed of pilot mode (M9). Other learnings: vite must be fully restarted (preview_stop/start) if vite.config.ts appears after server start; flat-color procedural rock under fog reads as featureless mush — vertex-color noise + flatShading fixed it (carry into M2's shader thinking); placeholder renders at 0.03 ms/frame. PowerShell note: never put double quotes inside git commit messages (PS 5.1 arg-passing mangles them).
- **2026-07-21 — M11: the user's redesign round, part 1 (docs + audio quick fixes).** The user reopened the design after real play, and the verdict is structural: killing-for-points made combat a chore, so the POINTS ECONOMY IS DELETED (found items: dynamite/keys/flasks/slugs; the Abyss hatch tolls +5 shifts), ROUNDS become time-based shifts with a map-wide wandering/despawning population (winnable with zero kills), the Angler becomes a patrol-freeze-vortex predator plus a NEW standing Lamp Man sharing its exact lure, the VO doctrine flips to in-head (Lowe's lines were never spoken aloud — LORE §2.1 rewrite keeps his mouth shut underwater while lines play mid-swim; REMORA goes robotic and deliberately maybe-imaginary), music/voice get single-slot arbiters, and a Museum Annex + Concept Gallery celebrate the story. All captured as M12–M16 with M17 parked (weapon feel). LORE v3's one law: SPREAD THE WRONGNESS — the count thread (42) is grandfathered and closed to new material; new voids #9 (the lamp) and #10 (the bells) plus 21 new lines carry other threads. CANON.md born (built ↔ lore trace, upkeep rule in the session protocol). Quick fixes shipped: SampleBank peak-normalization (the unheard box/PaP/round sounds were quiet generated masters — boost-only ×16 cap, computed at decode), amb-machinery regenerated as one enormous slow distant pump, Still on Shift pinned first on jukebox wake. Gemini image quota re-probed: STILL 0 — concept art waits on Google billing (script pattern specced in LORE §7.1). powerGate question answered for the user: the checkbox turns an edge into a gate that grinds open automatically when the Pile goes live (no cost, no key — the current map has none).
- **2026-07-21 — The image round (user enabled Gemini billing mid-session).** Probe-first paid off again: one tiny request confirmed the quota, then two background runs generated everything — 12/12 concept paintings (`scripts/generate-concept.mjs`, new, LORE §7.1 prompts) and 22/22 in-world images (the M8c script unchanged). Two failures, both instructive: g7 returned `finishReason: NO_IMAGE` once and succeeded verbatim on retry (transient — retry before rewording), and g12-sign's 3:1 aspect is simply unsupported (allowed set is the 1:1/2:3/3:2/3:4/16:9/21:9 family; 4:1 also rejected; 21:9 landed). The CONCEPT ART section shipped alongside (M16's bullet, early): `src/game/concept.ts` (manifest loader + procedural FILM UNDEVELOPED frames so the gallery never blocks) + a `concept` menu screen on title AND pause with lightbox. Verified by DOM drive (12 tiles all generated, lightbox open/close, BACK) — the hidden-pane screenshot timeout struck again, DOM assertions carried it; media.ts auto-upgrade confirmed via imageUrl() returning /images/*.png for g2/g3/g12-sign. Art spot-check by eye: c7 Lamp Man and c6 Angler are ON brief; g8's MISSING notice lettering came out fully legible (the fallback-caption safety net wasn't even needed there). ~60 MB of PNGs committed — static-asset rule §14 holds (generate once, commit, runtime never calls out).
- **2026-07-21 — M12 complete (one voice, one song).** The audio-direction round: `speech.ts` (pure arbitrate: tape > active speaker > better head-of-queue, ties to Lowe) + `music.ts` (MusicDirector, ONE slot, injectable track factory so vitest needs no Audio element) now own every voice and every song; director.playMusic deleted; the jukebox and the lull were refactored onto the slot and the menus theme YIELDS to any game song (the pause-over-jukebox double-song is dead too, not just the lull collision). The in-head doctrine landed as DSP, not new assets: Lowe re-routes through a warmth-lowpass+compressor+whisper-double chain, REMORA through a 320–3300 Hz telephone band + 52 Hz ring-mod — measured offline (REMORA crushes 100 Hz ×0.17, tremolo depth 0.28; Lowe passes lows full, damps 8 kHz). VoiceQueue's gate param became caller-semantic (Lowe: alive-anywhere; REMORA: submerged) — the M8b tests didn't change, which was the design's point. Verification lessons: (1) harness state PERSISTS across javascript_tool runs — a crashed run had already latched ctl.moonlightStarted and "the trigger didn't fire" was the trigger having fired; reload before re-proving one-shot latches. (2) A 900-frame live drive showed REMORA legally monopolizing the slot through her queued backlog — one-voice means exactly that; if her backlog ever feels chatty in play it's a tuning matter (queue caps), not an arbiter bug. (3) `bw.player.pos` doesn't exist — depth reads come from bw.camera.position. Bell generated + normalized (×1.4); bell.2/rem.hatch.1/lamp.1/museum.1/party.1 sit generated-but-dormant for M13/M15/M16. 138/138 tests; console clean.
- **2026-07-21 — M13a: the free cave, economy core (survived a mid-session crash; M13 split per protocol).** The points economy is dead and the game did not need it: guns are free issue, ammo/supplies/box obey ONE PULL PER MAN PER BELL (`BellIssue` — the pacing device that replaced price; the box tease REFUNDS your bell, which is a nicer apology than 950 points ever was), draughts are take-and-drink with power, doors want what the world hides (4 dynamite, 2 tagged keys on hook boards with more hooks than keys, 3 slugs), and the hatch charges the only currency the site respects — five bells, +5 shifts, horn suppressed so the bells own it. **Crash recovery notes:** the Anthropic-side interruption killed the session mid-edit AND took the Browser pane's MCP tools with it on resume; the working tree held (layout pickups + new modules + half of shops.ts survived on disk — git status was the honest map of where I stopped; trust the tree, not memory). With no pane, the DoD went HEADLESS: `freecave.test.ts` drives pickups→debris→grate→hatch→locker→box→rack→Bench through the REAL constructors (THREE builds object graphs fine in node; the only DOM the economy touches is 2D-canvas labels, stubbed with one chainable Proxy) — arguably a stronger net than the manual pass it replaced, and it stays forever. The browser smoke (props on rock, belt HUD, audible bells) is explicitly OWED and lives as an open bullet at M13b. Design note for the record: keys are KEPT after use (a used key is a story, not a resource); `startPoints` still seeds the ledger display — it's Lowe's count, not spending power. 153/153; tsc + production build clean.
- **2026-07-21 — M13b: the free cave, editor round (+ the paid smoke).** The M13a smoke came first and found the session's one real gotcha AGAIN: the new pickups "didn't exist" in the live game because **vite deliberately never watches layout.json (the M4.8 HMR guard) — a long-running dev server serves the STALE module even to fresh page loads; restart the server after any out-of-editor layout change** (now a ⚠ in STATUS because two sessions have now tripped on it). Smoke results: the whole found-item flow works live exactly as the headless suite promised — crate/key/slug takes update the belt HUD correctly (`DYN ×1 · KEY: PILE GRATE · SLUG ×1`), the choke consumes its charge, the grate names its key in the prompt, the hatch rolled rounds 1→6 with the five-bell toast; all pickup rest positions SDF-probed open. Scripted-verification lessons relearned with new specifics: the player controller CLOBBERS camera orientation every tick (position sticks, lookAt doesn't — aim with `bw.look(yaw,pitch)`, yaw needs the +π flip), and the editor already had its own `__bwEdit.shot()` render-then-capture helper (a bare canvas.toDataURL on the WebGL canvas is blank — no preserveDrawingBuffer). Editor work: ⬭ tunnel view (per-segment cylinders at real width radii — honest to the capsule sweep and cheap; knee spheres at bends; door rings via the same doorPlacement math the game uses, so the marker IS the door's true spot), pickup form with keyFor sourced from live EDGES, and glyph labels. All view meshes ship with raycast disabled — the opacity-0-writes-depth bug family stays dead. 28/28 editor checks, 153/153 tests, tsc clean.
- **2026-07-21 — M14: the living cave (+ two user ideas planned: M14.5 the Roster of 41, M15.5 the Undertow).** Rounds are gone; the watch keeps itself now. The session's real story is THREE population pathologies caught live by census instead of eyeball, each with a one-line mechanism: (1) any distance bias in burrow choice — even jittered — funnels the entire cave through the player's nearest burrows (16/16 hunters, zero wanderers) → UNIFORM pick over valid burrows; (2) with doors closed the wander graph is PARTITIONED, so random cross-cave targets nearly always fail and wanderers idle (0/9 with targets) → retry + a guaranteed one-hop stroll, and the escape leg from a squeeze-only burrow may use ANY open edge (the squeeze ban is for loitering, not for leaving); (3) despawn-respawn cycles RATCHET the population into hunters camped on the player (23/24) because hunters never despawn and near-spawns aggro instantly → the population spawner's distance floor sits just above aggro range, so every fresh body starts a wanderer and pressure arrives by DRIFTING IN — which was the user's fiction all along. Pack paranoia honored by construction (staggered emergence from one proven point — no new placement math exists to be wrong; DESIGN §9 updated with the deviation). Perf: the feared 24-alive cost is 0.64 ms on a fresh page — the earlier scary 8.3 ms and 5.0 ms readings were the poisoned-bench artifact (M5 lesson) plus the all-hunters pathology, in that order; measure AFTER fixing the population, on a fresh page, or you will optimize ghosts. Zero-kill win driven live to the ledger beat. The census-then-mechanism debug pattern (mode/zone counts over screenshots) is the keeper: populations lie to the eye and confess to a histogram. 150/150; tsc clean.
- **2026-07-21 — M14.5: the Roster of 41.** The crew book lives (`src/zombies/roster.ts`): 41 men, T6's read verbatim at the top (Barrow is LAMPS — the canon handed us the dry-cell carrier for free, and Marsh/pile watch + Bell/stores complete the three), everything about a man derived deterministically from `mulberry32(1000 + i×7919)`. The load-bearing correctness decision: **a man's slot frees on `remove()`, not on death** — so a drifting corpse blocks his own respawn (observed live: Bell spawned NULL while his body drifted, walked again after the fade), which is exactly the fiction. Drops route through the existing world-drop system (CELL/MAX AMMO reused; new `fuelSlug` DropId excluded from the random roll — `Object.keys(weights)` was already the roll domain, so exclusion = simply not adding a weight). The battery shelf STAYS until the user plays (Barrow at weight 0.35 may or may not cover the battery economy alone — a availability question with a feel component, so it's theirs). Moan masters NOT regenerated: 3 samples × per-man 0.78–1.22 playbackRate reads as distinct voices in the director log; if the spread wears thin on real speakers, generating moan-4/5 is a one-line script run. **Verification lessons:** (1) the preview pane can silently collapse the canvas to 0×0 — `renderer.setSize(1280,720,false)` before ANY `bw.shot()` or bench, and treat every historical ms/frame number (M14's 0.64 included) as hidden-canvas noise; the real Intel UHD at 720p reads ~4 ms empty / ~14 ms at 22 alive ≈ 60–70 fps (budget holds, but thinner than we believed); (2) with the pane visible the rAF loop runs BETWEEN javascript_tool calls — pose-then-shoot must happen inside one call or the men swim out of frame; (3) `bw.look` takes DEGREES (the M13b radians+π flip habit aimed 0.994-dot by luck). Vite port lesson re-tripped: preview_start reported 50371, vite actually served 5174 — read preview_logs first, always. 160/160; tsc clean; console clean.
